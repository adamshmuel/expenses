/**
 * The design-time half of every test, inlined here so the HTML report is
 * self-contained: purpose, what is under test, method (setup + exact inputs),
 * expected result. Keyed by test ID. The report matches Vitest results to these
 * by the ID prefix in each test title.
 *
 * This mirrors qa/specs/*.md — the prose source of record. If you change a test
 * spec, update the matching entry here.
 */

export const GOVERNING = {
  US: { file: "docs/specs/05-user-layers.md", sec: "§4, §10 decision 1", specFile: "qa/specs/unit-userService.md", src: "qa/tests/unit/userService.test.ts" },
  RA: { file: "docs/specs/06-server-modules.md", sec: "§4", specFile: "qa/specs/unit-requireAuth.md", src: "qa/tests/unit/requireAuth.test.ts" },
  EH: { file: "docs/specs/06-server-modules.md", sec: "§3 (+ 03 §0)", specFile: "qa/specs/unit-errorHandling.md", src: "qa/tests/unit/errorHandling.test.ts" },
  LG: { file: "docs/specs/06-server-modules.md", sec: "§1 (+ 01 §9)", specFile: "qa/specs/int-logger.md", src: "qa/tests/integration/logger.test.ts" },
  MD: { file: "docs/specs/04-data-model.md", sec: "User / RefreshToken / Expense", specFile: "qa/specs/int-models.md", src: "qa/tests/integration/models.test.ts" },
  DB: { file: "docs/specs/06-server-modules.md", sec: "§2, §6 decision 3", specFile: "qa/specs/int-db.md", src: "qa/tests/integration/db.test.ts" },
  ST: { file: "docs/specs/07-server-entry.md", sec: "§1 (+ 06 §2)", specFile: "qa/specs/int-startup.md", src: "qa/tests/integration/startup.test.ts" },
  SU: { file: "docs/specs/03-api-contract.md", sec: "§0-§1 (+ 05 §5-§8, §10 d4)", specFile: "qa/specs/api-signup.md", src: "qa/tests/api/01-signup.test.ts" },
  RF: { file: "docs/specs/03-api-contract.md", sec: "§1 (+ 05 §6/§8, 04, 06 §3.3)", specFile: "qa/specs/api-refresh.md", src: "qa/tests/api/02-refresh.test.ts" },
  LO: { file: "docs/specs/03-api-contract.md", sec: "§1 (+ 05 §6)", specFile: "qa/specs/api-logout.md", src: "qa/tests/api/03-logout.test.ts" },
  XC: { file: "docs/specs/03-api-contract.md", sec: "§0 (+ 07 §2/§4, 05 §5, 06 §3)", specFile: "qa/specs/api-crosscutting.md", src: "qa/tests/api/04-crosscutting.test.ts" },
  LI: { file: "docs/specs/03-api-contract.md", sec: "§1 (+ 05 §6-§8)", specFile: "qa/specs/api-login.md", src: "qa/tests/api/05-login.test.ts" },
  UJ: { file: "docs/specs/01-ai-chat.md", sec: "§1, §4 (+ 03 §1, 05 §4/§6)", specFile: "qa/specs/e2e-auth-journey.md", src: "qa/e2e/tests/auth-journey.spec.ts" },
  CS: { file: "docs/specs/09-expense-category-service.md", sec: "§2", specFile: "qa/specs/unit-categoryService.md", src: "qa/tests/unit/categoryService.test.ts" },
  ES: { file: "docs/specs/09-expense-category-service.md", sec: "§3", specFile: "qa/specs/unit-expenseService.md", src: "qa/tests/unit/expenseService.test.ts" },
  AI: { file: "docs/specs/01-ai-chat.md", sec: "§6, §8", specFile: "qa/specs/unit-ai.md", src: "qa/tests/unit/ai.test.ts" },
  CH: { file: "docs/specs/04-data-model.md", sec: "Category (+ 09 §2)", specFile: "qa/specs/int-categoryModel.md", src: "qa/tests/integration/categoryModel.test.ts" },
  CI: { file: "docs/specs/09-expense-category-service.md", sec: "§2 (+ 04-data-model.md Category)", specFile: "qa/specs/int-categoryService.md", src: "qa/tests/integration/categoryService.test.ts" },
  EI: { file: "docs/specs/09-expense-category-service.md", sec: "§3", specFile: "qa/specs/int-expenseService.md", src: "qa/tests/integration/expenseService.test.ts" },
  DL: { file: "docs/specs/08-expense-category-dal.md", sec: "Category + Expense functions", specFile: "qa/specs/int-expenseCategoryDal.md", src: "qa/tests/integration/expenseCategoryDal.test.ts" },
  CC: { file: "docs/specs/09-expense-category-service.md", sec: "§4 (+ 01-ai-chat.md §6)", specFile: "qa/specs/api-chat-confirm.md", src: "qa/tests/api/06-chat-confirm.test.ts" },
  CM: { file: "docs/specs/01-ai-chat.md", sec: "§5, §6, §9", specFile: "qa/specs/api-chat-messages.md", src: "qa/tests/api/07-chat-messages.test.ts" },
  ER: { file: "docs/specs/09-expense-category-service.md", sec: "§5", specFile: "qa/specs/api-expenses-categories.md", src: "qa/tests/api/08-expenses-categories.test.ts" },
  FJ: { file: "docs/specs/01-ai-chat.md", sec: "§6-§7 (+ 02-dashboard.md)", specFile: "qa/specs/e2e-chat-dashboard-journey.md", src: "qa/e2e/tests/chat-dashboard-journey.spec.ts" },
  DJ: { file: "docs/specs/02-dashboard.md", sec: "whole file", specFile: "qa/specs/e2e-chat-dashboard-journey.md", src: "qa/e2e/tests/chat-dashboard-journey.spec.ts" },
  CR: { file: "docs/specs/09-expense-category-service.md", sec: "§2-§4 (+ 01-ai-chat.md §6-§7)", specFile: "qa/specs/api-chat-confirm-resolution.md", src: "qa/tests/api/09-chat-confirm-resolution.test.ts" },
  FS: { file: "docs/specs/01-ai-chat.md", sec: "§6-§7", specFile: "qa/specs/e2e-chat-stress-flows.md", src: "qa/e2e/tests/chat-stress-flows.spec.ts" },
  // ---- 2026-09-16 redesign pass: fresh-account flow + lived-in accounts ----
  FR: { file: "docs/specs/01-ai-chat.md, 02-dashboard.md", sec: "whole app, user-first redesign pass", specFile: "qa/specs/flow-fresh-2026-09-16.md", src: "qa/e2e/tests/fresh-*.spec.ts" },
  XS: { file: "docs/specs/01-ai-chat.md §6-§7, 03-api-contract.md", sec: "Group 6/12 — boundaries reachable only past the UI", specFile: "qa/specs/flow-fresh-2026-09-16.md", src: "qa/e2e/tests/fresh-hostile.spec.ts, fresh-input-space.spec.ts + qa/tests/api/10-fresh-boundaries.test.ts, 13-fresh-split-halves.test.ts" },
  BD: { file: "docs/reference/testing-reference/2026-09-16-manual-bugs-found.md, 2026-09-16-chat-flow-bugs-and-fixes.md", sec: "Group 13 — classes learned from the eleven escaped bugs", specFile: "qa/specs/flow-fresh-2026-09-16.md", src: "qa/e2e/tests/fresh-bug-classes.spec.ts, lived-in-flows.spec.ts + qa/tests/api/11-ai-server-contract.test.ts, 12-observability.test.ts, 13-fresh-split-halves.test.ts" },
  LV: { file: "docs/specs/01-ai-chat.md, 02-dashboard.md", sec: "whole app, at volume with real history", specFile: "qa/specs/flow-lived-in-2026-09-16.md", src: "qa/e2e/tests/lived-in-flows.spec.ts" },
  // ---- 2026-09-17 pass: nine bug fixes re-verified + routing/shared-component findings ----
  RG: { file: "docs/specs/01-ai-chat.md, 04-data-model.md, 06-server-modules.md, 07-server-entry.md", sec: "regression cases pinned to the nine 2026-09-17 fixes", specFile: "qa/specs/regression-2026-09-17.md", src: "qa/tests/api/15-regression-2026-09-17.test.ts, qa/tests/unit/regression-2026-09-17.test.ts, qa/e2e/tests/regression-2026-09-17.spec.ts" },
  RT: { file: "client/src/store/authSlice.ts, ProtectedRoute.tsx", sec: "protected-route reload/redirect defect", specFile: "qa/specs/exploratory-2026-09-17.md", src: "qa/e2e/tests/regression-2026-09-17.spec.ts" },
  SC: { file: "client/src/components/ChatScreen.tsx, lib/dashboardFormat.ts", sec: "§1-§2 (money formatter + import graph)", specFile: "qa/specs/client-shared-components.md", src: "qa/tests/unit/sc-static.test.ts" },
};

export const GROUPS = [
  { key: "US", title: "Unit — bl/userService.js" },
  { key: "RA", title: "Unit — middleware/requireAuth.js" },
  { key: "EH", title: "Unit — error_handling.js" },
  { key: "LG", title: "Integration — .config/logger.js" },
  { key: "MD", title: "Integration — Mongoose models" },
  { key: "DB", title: "Integration — .config/db.js (connectDB)" },
  { key: "ST", title: "Integration — index.js start-up" },
  { key: "SU", title: "API — POST /users/signup" },
  { key: "LI", title: "API — POST /users/login" },
  { key: "RF", title: "API — POST /users/refresh" },
  { key: "LO", title: "API — POST /users/logout" },
  { key: "XC", title: "API — cross-cutting (contract, headers, CORS, 404)" },
  { key: "UJ", title: "E2E — client + server auth journeys (browser)" },
  { key: "CS", title: "Unit — bl/categoryService.js" },
  { key: "ES", title: "Unit — bl/expenseService.js" },
  { key: "AI", title: "Unit — backend/ai/ (prompt, schema, parseMessage)" },
  { key: "CH", title: "Integration — models/categoryModel.js hooks" },
  { key: "CI", title: "Integration — categoryService (seed/reset defaults)" },
  { key: "EI", title: "Integration — expenseService.createManyExpenses" },
  { key: "DL", title: "Integration — expense/category DAL additions" },
  { key: "CC", title: "API — POST /chat/confirm" },
  { key: "CM", title: "API — GET/POST /chat/messages" },
  { key: "ER", title: "API — GET /expenses, /expenses/summary, /categories" },
  { key: "FJ", title: "E2E — chat -> dashboard flagship journey (real AI, browser)" },
  { key: "DJ", title: "E2E — dashboard reflects recorded expenses (browser)" },
  { key: "CR", title: "API — POST /chat/confirm category resolution + audit trail (2026-09-16 pass)" },
  { key: "FS", title: "E2E — chat stress flows: multi-turn, correction, batch, bad category, reload, logout, 'yes' (2026-09-16 pass)" },
  { key: "FR", title: "E2E — fresh-account flow, user-first redesign (2026-09-16 pass)" },
  { key: "XS", title: "API/Browser — fresh-account boundaries reachable only past the UI (2026-09-16 pass)" },
  { key: "BD", title: "Bug-derived classes, aimed at parts of the app the eleven escaped bugs never touched (2026-09-16 pass)" },
  { key: "LV", title: "E2E — lived-in accounts at volume (2026-09-16 pass)" },
  { key: "RG", title: "API/Unit/E2E — regressions for the nine 2026-09-17 bug fixes" },
  { key: "RT", title: "E2E — protected-route reload/redirect defect (2026-09-17 exploratory)" },
  { key: "SC", title: "Unit (static) — shared money formatter + import-graph guard (2026-09-17)" },
];

/** id -> { purpose, under, method, expected, divergence? } */
export const CASES = {
  // ---- US ----
  "US-01": { purpose: "The access token carries only what requireAuth reads (req.user = {id, username}).", under: "issueTokenPair via signup(); jwt real.", method: "Mock createUser -> {_id:'u1', username:'amir', email:'a@x.io'}. bcrypt.hash -> 'HASHED::pw'. Call signup({username:'amir', email:'a@x.io', password:'password123'}). jwt.decode the accessToken.", expected: "Payload keys are exactly id, username, iat, exp. id==='u1', username==='amir'. No email, no password." },
  "US-02": { purpose: "The refresh token is minimal and does not carry the username.", under: "issueTokenPair via signup().", method: "As US-01; jwt.decode the refreshToken.", expected: "Payload keys are exactly userId, iat, exp. userId==='u1'." },
  "US-03": { purpose: "Access and refresh secrets are separate (spec §10 d1) — a leaked access secret must not verify a refresh token.", under: "issueTokenPair.", method: "JWT_SECRET='test-access-secret', REFRESH_TOKEN_SECRET='test-refresh-secret'. Run signup. Verify each token under each secret.", expected: "accessToken verifies only under the access secret; refreshToken only under the refresh secret; the cross cases throw." },
  "US-04": { purpose: "Token lifetimes match spec §10 decision 1.", under: "issueTokenPair.", method: "Run signup; decode both tokens; compute exp-iat.", expected: "access exp-iat === 900 (±2s); refresh exp-iat === 604800 (±2s)." },
  "US-05": { purpose: "The TTL index gets a correct date to work from.", under: "issueTokenPair -> saveRefreshToken.", method: "Freeze time at 2026-01-15T12:00:00Z. Run signup. Inspect the saveRefreshToken argument.", expected: "Called once with { token:<string>, user:'u1', expiresAt:<Date> } where expiresAt.getTime() === Date.now() + 7*24*60*60*1000 exactly." },
  "US-06": { purpose: "The repository never receives a plain password.", under: "signup().", method: "bcrypt.hash spy -> 'HASHED::'+input. Call signup with password:'plaintextpw'. Inspect bcrypt.hash and createUser calls.", expected: "bcrypt.hash called once with ('plaintextpw', 10). createUser called once with password === 'HASHED::plaintextpw'." },
  "US-07": { purpose: "The route gets { user, accessToken, refreshToken }.", under: "signup() return value.", method: "Run signup, inspect the resolved object.", expected: "Keys exactly user, accessToken, refreshToken. user is the createUser result. Both tokens are non-empty strings." },
  "US-08": { purpose: "The service does not swallow or remap E11000 — the central handler does (spec §8, §10 d3).", under: "signup() error propagation.", method: "createUser rejects with Object.assign(new Error('E11000 dup key'), {code:11000}). Run signup.", expected: "signup rejects with the SAME error object; .code===11000 intact; saveRefreshToken never called." },
  "US-09": { purpose: "Happy-path login (spec §4 login).", under: "login().", method: "findUserByUsername -> user with password:'HASHED'. bcrypt.compare -> true. Call login('amir','rightpw').", expected: "Resolves { user, accessToken, refreshToken }; user._id==='u1'; bcrypt.compare called with ('rightpw','HASHED')." },
  "US-10": { purpose: "The response must not reveal which credential was wrong (spec §4 step 2, §8).", under: "login() failure paths.", method: "Case A: findUserByUsername -> null; login('ghost','x'). Case B: user present, bcrypt.compare -> false; login('amir','wrong').", expected: "Both reject with .status===401 and message 'Username or password is incorrect.' — byte-identical. Case A does not call bcrypt.compare. No token issued in either." },
  "US-11": { purpose: "Rotation (spec §4 refresh steps 1-6).", under: "refresh() happy path.", method: "findRefreshToken -> truthy row. Pre-sign a valid refresh token for {userId:'u1'}. findUserById -> user. deleteRefreshToken resolves. Call refresh(oldToken).", expected: "Resolves { user, accessToken, refreshToken }. deleteRefreshToken called once with oldToken. saveRefreshToken called once with a DIFFERENT token. user._id==='u1'." },
  "US-12": { purpose: "A rotated-out or pre-restart token is rejected before jwt.verify (spec §4 refresh step 1).", under: "refresh() unknown token.", method: "findRefreshToken -> null. Call refresh('whatever').", expected: "Rejects with .status===401, message 'not recognized'. findUserById / deleteRefreshToken never called." },
  "US-13": { purpose: "A tampered/expired token that is somehow in the collection still fails; rotation aborts before the delete (spec §4 refresh step 2).", under: "refresh() jwt.verify failure.", method: "findRefreshToken -> truthy row. Pass 'not.a.jwt' so real jwt.verify throws. Call refresh('not.a.jwt').", expected: "Rejects with an Error. deleteRefreshToken / saveRefreshToken never called." },
  "US-14": { purpose: "End-to-end rotation property at the unit level.", under: "refresh() called twice with the same token.", method: "First call: full happy path, deleteRefreshToken resolves. Then findRefreshToken.mockResolvedValue(null). Call refresh(old) again.", expected: "First call resolves; second call rejects with .status===401 / 'not recognized'." },
  "US-15": { purpose: "Logout invalidates the refresh token (spec §4 logout).", under: "logout().", method: "deleteRefreshToken resolves. Call logout('tok123').", expected: "deleteRefreshToken called once with 'tok123'. Resolves to undefined." },
  "US-16": { purpose: "Logout is idempotent / safe when no cookie was sent (spec §4 logout).", under: "logout(undefined).", method: "deleteRefreshToken resolves. Call logout(undefined).", expected: "Does not throw; resolves. deleteRefreshToken called once with undefined." },
  "US-17": { purpose: "The private helper stays private (spec §4 'not exported').", under: "userService module exports.", method: "require the module and inspect its keys.", expected: "Exported keys are exactly signup, login, refresh, logout. No issueTokenPair." },
  "US-18": { purpose: "Login creates a RefreshToken row (spec 04 'RefreshToken': Login creates one).", under: "login() -> issueTokenPair -> saveRefreshToken.", method: "As US-09; inspect saveRefreshToken.", expected: "Called once with { token:<string>, user:'u1', expiresAt:<Date ~7d out> }." },
  "US-19": { purpose: "Every new user starts with the shared default category set, created as part of signup (spec 09 §2).", under: "signup() -> categoryService.seedDefaultCategories.", method: "categoryService.seedDefaultCategories stubbed to resolve []. createUser -> FAKE_USER (_id:'u1'). Call signup(...).", expected: "seedDefaultCategories called once, with 'u1'." },

  // ---- RA ----
  "RA-01": { purpose: "An unauthenticated request never reaches the route (spec §4 step 2).", under: "requireAuth with no Authorization header.", method: "req.headers = {}. Call requireAuth(req,res,next).", expected: "res.status(401), res.json({error:'Not authenticated.'}). next NOT called. req.user undefined." },
  "RA-02": { purpose: "A malformed auth scheme is rejected (spec §4 step 2).", under: "requireAuth with 'Token abc123'.", method: "authorization:'Token abc123'. Call requireAuth.", expected: "401 {error:'Not authenticated.'}. next NOT called." },
  "RA-03": { purpose: "'Bearer ' with an empty token must not verify (spec §4 step 3 body).", under: "requireAuth with 'Bearer ' (empty token).", method: "authorization:'Bearer '. Call requireAuth.", expected: "401, body { error:'Not authenticated.' } per spec §4 step 3. (Shares the code's catch string with RA-06.)" },
  "RA-04": { purpose: "The guard attaches only the two fields routes need, not the raw JWT payload (spec §4 step 4).", under: "requireAuth with a valid token.", method: "Sign {id:'u1', username:'amir'} with JWT_SECRET, 15m. authorization:'Bearer <token>'. Call requireAuth.", expected: "next called exactly once with no arg. req.user deep-equals {id:'u1', username:'amir'} — NO iat, NO exp. res.status/json not called." },
  "RA-05": { purpose: "An expired access token is rejected (spec §4 step 3).", under: "requireAuth with an expired token.", method: "Sign with { expiresIn:-10 }. authorization:'Bearer <expired>'. Call requireAuth.", expected: "res.status(401). next not called." },
  "RA-06": { purpose: "Spec 06 §4 step 3 fixes the failed-verify body as { error:'Not authenticated.' }, identical to the missing-header case.", under: "requireAuth with a wrong-secret token.", method: "jwt.sign(payload,'wrong-secret'). authorization:'Bearer <tampered>'. Call requireAuth.", expected: "Per spec: res.json({error:'Not authenticated.'}). Actual code (line 41): { error:'Invalid or expired token!' }. Status 401 and no-next are correct; only the string violates the spec.", divergence: "SPEC DIVERGENCE — message string. Fix: change requireAuth.js line 41 to { error: 'Not authenticated.' }, or amend spec 06 §4." },
  "RA-07": { purpose: "A token with a mutated signature does not pass.", under: "requireAuth with a flipped signature byte.", method: "Take a valid token, flip the last char of the signature segment. authorization:'Bearer <mutated>'. Call requireAuth.", expected: "res.status(401). next not called. req.user undefined." },
  "RA-08": { purpose: "The token is self-contained; requireAuth must not hit Mongo (spec §4 'Does not touch the database').", under: "requireAuth.js source.", method: "Read the file; list every require() specifier.", expected: "The only require is 'jsonwebtoken'. No models/, dal/, or mongoose reference." },
  "RA-09": { purpose: "Documents the exact mapping: req.user = { id: decoded.id, username: decoded.username } — two named fields, no validation.", under: "requireAuth with a token whose payload lacks username.", method: "Sign { id:'u1' } only. Call requireAuth.", expected: "next called once. req.user deep-equals { id:'u1', username: undefined }." },

  // ---- EH ----
  "EH-01": { purpose: "An async throw does not crash the process; it reaches the central handler (spec §3.1).", under: "catchAsync with a rejecting handler.", method: "const boom = new Error('kaboom'); wrapped = catchAsync(async () => { throw boom }); call wrapped(req,res,next); await a tick.", expected: "next called exactly once with boom (same reference). res.status/json never called." },
  "EH-02": { purpose: "The wrapper is transparent on the happy path.", under: "catchAsync with a resolving handler.", method: "wrapped = catchAsync(async (req,res) => res.json({ok:true})); call it; await a tick.", expected: "next not called. res.json({ok:true}) called once by the handler." },
  "EH-03": { purpose: "Logging happens once, in errorHandler only (spec §3.3, decision 2 — no double logging).", under: "catchAsync.", method: "As EH-01; inspect the logger spies after the tick.", expected: "logger.warn / error / info / http all zero calls." },
  "EH-04": { purpose: "Promise.resolve(handler(...)) also catches a sync throw inside an async handler; documents the boundary.", under: "catchAsync with a synchronously-throwing async handler.", method: "wrapped = catchAsync(async () => { JSON.parse('{') }); call it; await a tick.", expected: "next called once with a SyntaxError." },
  "EH-05": { purpose: "Duplicate-key errors become a 409 here, once, for every unique-indexed model (spec §3.2, 05 d3).", under: "errorHandler with err.code === 11000.", method: "err = Object.assign(new Error('E11000 duplicate key error ... username_1'), {code:11000}); call errorHandler(err,req,res,next).", expected: "res.status(409). res.json called once with an object whose only key is error (a string). logger.warn once; logger.error not called.", divergence: "SPEC WORDING NOTE — spec §3.2 table gives the message as '<field> already exists.'; code returns 'That username or email is already taken.'. Status + shape are correct and asserted; wording divergence recorded, not failed." },
  "EH-06": { purpose: "A deliberate error (e.g. 401 from userService.login) is passed through with its chosen message (spec §3.2 row 2, §3.3).", under: "errorHandler with err.status set.", method: "err = Object.assign(new Error('Username or password is incorrect.'), {status:401}); call errorHandler.", expected: "res.status(401), res.json({error:'Username or password is incorrect.'}). logger.warn once; logger.error not called." },
  "EH-07": { purpose: "Deliberate 4xx keep error.log clean (spec §3.3).", under: "errorHandler with a 400 err.status.", method: "err = Object.assign(new Error('bad'), {status:400}); call errorHandler.", expected: "res.status(400), res.json({error:'bad'}). logger.warn once, logger.error zero." },
  "EH-08": { purpose: "An unexpected error is a 5xx and is logged as a real problem (spec §3.2 row 3, §3.3).", under: "errorHandler with no status/code.", method: "err = new Error('connection reset by peer'); call errorHandler.", expected: "res.status(500), res.json({error:'Something went wrong.'}). logger.error once; logger.warn not called." },
  "EH-09": { purpose: "The real failure message is logged, not sent to the client (spec §3.2 row 3).", under: "errorHandler 500 branch body.", method: "err = new Error('SECRET table users missing column ssn'); call errorHandler; inspect the res.json argument.", expected: "res.json argument is exactly { error:'Something went wrong.' }. 'SECRET' does not appear in the JSON argument. logger.error's first argument DOES contain the real message." },
  "EH-10": { purpose: "The handler only ever emits the general shape; { errors:[] } is produced earlier by validators and never reaches here (spec §3.2 'Key points').", under: "errorHandler across all three branches.", method: "Run the 11000, err.status, and fallback branches; collect each res.json argument.", expected: "Each argument has exactly one own key, error, a non-empty string. No errors key anywhere." },
  "EH-11": { purpose: "No double logging (decision 2).", under: "errorHandler logging count.", method: "For each branch, sum all logger spy call counts after one errorHandler call.", expected: "Total logger calls === 1 in every branch." },

  // ---- LG ----
  "LG-01": { purpose: "The separate error log is a filter, not a redirect — errors still go to the everything-log (spec §1).", under: "logger.error line routing.", method: "chdir into a temp dir, require a fresh logger. logger.error('disk fell off', {path:'/x'}). flush. Read logs/error.log and logs/app.log.", expected: "Both files contain a line with 'disk fell off' and [ERROR]." },
  "LG-02": { purpose: "error.log stays a list of real problems (spec §3.3 rationale, §1 table).", under: "logger.warn line routing.", method: "logger.warn('recovered ok'). flush. Read both files.", expected: "app.log contains 'recovered ok' [WARN]; error.log does NOT contain 'recovered ok'." },
  "LG-03": { purpose: "The AI-call log is fed by the area==='ai' filter (spec §1, decision 1).", under: "logger.info with { area:'ai' }.", method: "logger.info('gemini call', {area:'ai', ms:42, ok:true}). flush. Read logs/ai.log.", expected: "ai.log contains a line with 'gemini call' and \"ms\":42." },
  "LG-04": { purpose: "ai.log on its own is the uncluttered AI history (spec §1).", under: "logger.info with no area.", method: "logger.info('server started'). flush. Read logs/ai.log.", expected: "ai.log does NOT contain 'server started'. (app.log does.)" },
  "LG-05": { purpose: "AI lines still also appear in logs/app.log (spec §1).", under: "logger.info with { area:'ai' } -> app.log.", method: "From LG-03's write, read app.log.", expected: "app.log contains 'gemini call'." },
  "LG-06": { purpose: "The shared line format only appends JSON when there are extra fields (logger.js printf).", under: "logger.info with empty meta.", method: "logger.info('plain message'). flush. Read the matching line from app.log.", expected: "The line contains no { / } / {} — no trailing empty-object JSON." },
  "LG-07": { purpose: "A crash is never silent (spec §1 'Crash handlers').", under: "logger.exceptions / logger.rejections handlers.", method: "require the logger; inspect the exception and rejection handler transports' filenames.", expected: "At least one exception handler transport filename ends 'exception.log'; at least one rejection handler transport filename ends 'rejection.log'." },

  // ---- MD ----
  "MD-01": { purpose: "username is required (spec 04 User).", under: "userModel validation.", method: "new User({email:'a@x.io', password:'h'}).validate().", expected: "Rejects; err.errors.username present, kind 'required'." },
  "MD-02": { purpose: "username min length 3 (spec 04 User: 3-20).", under: "userModel validation.", method: "new User({username:'ab', email:'a@x.io', password:'h'}).validate().", expected: "Rejects; err.errors.username; message mentions '3 characters'." },
  "MD-03": { purpose: "username max length 20 (spec 04 User: 3-20).", under: "userModel validation.", method: "username = 21 chars. .validate().", expected: "Rejects; err.errors.username; message mentions '20'." },
  "MD-04": { purpose: "username is trimmed (spec 04 User).", under: "userModel setter.", method: "new User({username:'  amir  ', ...}); read .username.", expected: "'amir'." },
  "MD-05": { purpose: "email must pass validator.isEmail (spec 04 User: valid email).", under: "userModel validation.", method: "new User({username:'amir', email:'not-an-email', password:'h'}).validate().", expected: "Rejects; err.errors.email; message 'Please enter a valid email address'." },
  "MD-06": { purpose: "email is lowercased (spec 04 User).", under: "userModel setter.", method: "new User({..., email:'Amir@Example.IO', ...}); read .email.", expected: "'amir@example.io'." },
  "MD-07": { purpose: "username and email are unique (spec 04 User: required, unique).", under: "users collection indexes after User.init().", method: "await User.init(); User.collection.getIndexes({full:true}).", expected: "An index on {username:1} with unique:true, and one on {email:1} with unique:true." },
  "MD-08": { purpose: "createdAt is set automatically; toJSON exposes the id virtual (spec 04 User; 05 §5).", under: "userModel default + toJSON.", method: "Construct a User; check createdAt is a Date within 5s of now. Save; call toJSON().", expected: "createdAt recent Date. toJSON().id is a string equal to _id.toString()." },
  "MD-09": { purpose: "token is required (spec 04 RefreshToken).", under: "refreshTokenModel validation.", method: "new RefreshToken({user:<oid>, expiresAt:new Date()}).validate().", expected: "Rejects; err.errors.token, kind 'required'." },
  "MD-10": { purpose: "user is a required ObjectId ref to User (spec 04 RefreshToken).", under: "refreshTokenModel validation + schema.", method: "validate() without user; validate() with user:'not-an-oid'. Inspect schema path 'user'.", expected: "First rejects on user required; second rejects on cast error. Schema path user ref === 'User'." },
  "MD-11": { purpose: "expiresAt is required (spec 04 RefreshToken).", under: "refreshTokenModel validation.", method: "new RefreshToken({token:'t', user:<oid>}).validate().", expected: "Rejects; err.errors.expiresAt, kind 'required'." },
  "MD-12": { purpose: "token is unique (spec 04 RefreshToken).", under: "refreshtokens collection indexes.", method: "await RefreshToken.init(); getIndexes({full:true}).", expected: "An index on {token:1} with unique:true." },
  "MD-13": { purpose: "MongoDB removes expired tokens on its own (spec 04 RefreshToken: 'Put a TTL index on expiresAt').", under: "refreshtokens TTL index.", method: "getIndexes({full:true}); find the index whose key is {expiresAt:1}.", expected: "That index has expireAfterSeconds === 0." },
  "MD-14": { purpose: "user is indexed (spec 04 RefreshToken: required, indexed).", under: "refreshtokens indexes.", method: "getIndexes({full:true}).", expected: "An index whose key is {user:1}." },
  "MD-15": { purpose: "amount is required (spec 04 Expense).", under: "expenseModel validation.", method: "new Expense({category:<oid>, user:<oid>}).validate().", expected: "Rejects; err.errors.amount, kind 'required'." },
  "MD-16": { purpose: "amount must be greater than 0 (spec 04 Expense; model min 0.01).", under: "expenseModel validation.", method: "new Expense({amount:0, category:<oid>, user:<oid>}).validate().", expected: "Rejects; err.errors.amount; message mentions 'greater than 0'." },
  "MD-17": { purpose: "amount cannot be negative (spec 04 Expense).", under: "expenseModel validation.", method: "amount:-5. .validate().", expected: "Rejects; err.errors.amount." },
  "MD-18": { purpose: "amount of exactly 0.01 is accepted (model min is 0.01).", under: "expenseModel validation.", method: "new Expense({amount:0.01, category:<oid>, user:<oid>}).validate().", expected: "Resolves (no error)." },
  "MD-19": { purpose: "date defaults to today; category and user are required refs (spec 04 Expense).", under: "expenseModel defaults + validation + schema.", method: "Construct a valid Expense without date -> .date is a recent Date. new Expense({amount:5}).validate() -> rejects on category and user. Inspect schema path refs.", expected: "date recent Date; both category and user error; refs are 'Category' and 'User'." },
  "MD-20": { purpose: "Every dashboard query is 'this user, this period' (spec 04 Expense: index on user + date).", under: "expenses indexes.", method: "await Expense.init(); getIndexes({full:true}).", expected: "An index whose key is exactly {user:1, date:1}." },

  // ---- DB ----
  "DB-01": { purpose: "Happy path (spec §2 step 3).", under: "connectDB() with a good URI, in a child process.", method: "fork qa/harness/db-driver.js with MONGODB_URI = the derived test URI (.../expenses_qa_test), exit 0 after 4s. Capture stdout.", expected: "Child exits 0. Output contains 'connected to MongoDB' and [INFO]. No [ERROR]." },
  "DB-02": { purpose: "A server with no database must not stay up (spec §2 step 4, decision 3).", under: "connectDB() with a bad URI, in a child process.", method: "fork the driver with MONGODB_URI='mongodb://127.0.0.1:59999/nope?serverSelectionTimeoutMS=3000'. Wait for exit.", expected: "Child exits non-zero. Output contains 'Error connecting to MongoDB' and [ERROR]." },
  "DB-03": { purpose: "Scope (spec §2 'Not this module's job').", under: "db.js module exports.", method: "require('backend/.config/db.js') in-process; inspect exports; check mongoose.modelNames() is unchanged.", expected: "Exports are exactly { connectDB } (a function). require registers no Mongoose models." },

  // ---- ST ----
  "ST-01": { purpose: "spec §1 step 1 — require('dotenv').config() is the very first line.", under: "index.js start-up + static source.", method: "Static: first executable statement is require('dotenv').config(), before any other require. Behavioural: the shared server (spawned by the harness with PORT=3100) logged its readiness line; a POST /users/login with {} returns a well-formed 400.", expected: "First statement matches require('dotenv').config(); stdout contains 'Server running on http://localhost:3100'; the empty-body login returns 400." },
  "ST-02": { purpose: "spec §1 steps 6-7 — connect, then listen.", under: "index.js source order + running server.", method: "Static: connectDB() appears before app.listen( in index.js. Behavioural: GET /users/<unknown> returns a JSON 404 (not a connection error) and stdout also shows 'connected to MongoDB'.", expected: "connectIdx < listenIdx; the 404 response has content-type application/json; 'connected to MongoDB' present." },
  "ST-03": { purpose: "spec §1 step 6 / 06 §2 step 4 — no DB => no server.", under: "a fresh index.js spawn with a broken MONGODB_URI.", method: "spawn node backend/index.js with MONGODB_URI='mongodb://127.0.0.1:59999/nope?serverSelectionTimeoutMS=3000', PORT=3101. Send no requests. Wait up to 15s.", expected: "The child exits with a non-zero code (does not stay alive). Output contains 'Error connecting to MongoDB'. (The port may briefly bind before Mongoose gives up; the pass condition is process death via connectDB's process.exit(1).)" },

  // ---- SU ----
  "SU-01": { purpose: "Valid signup succeeds (spec 03 §1: 201 Created).", under: "POST /users/signup happy path.", method: "POST a fresh makeUser() body {username:'qa_<id>', email:'qa_<id>@test.io', password:'password123'}.", expected: "HTTP 201." },
  "SU-02": { purpose: "The success body is exactly { user:{id,username,email}, accessToken } (spec 03 §1, 05 §5).", under: "signup success body shape.", method: "Parse the SU-01 response; validate against AuthSuccess.strict().", expected: "Validation passes. Top-level keys exactly user, accessToken. user keys exactly id, username, email." },
  "SU-03": { purpose: "Password never leaves the server (spec 03 §1, 05 §5).", under: "signup body — secret leak.", method: "Stringify the whole body; search for 'password', '$2a$', '$2b$', '$2y$'.", expected: "None present." },
  "SU-04": { purpose: "spec 05 §10 decision 4 — response body is {id,username,email} only.", under: "signup body — createdAt.", method: "Inspect body.user.", expected: "No createdAt key." },
  "SU-05": { purpose: "The refresh token lives only in the cookie (spec 05 §5).", under: "signup body — refreshToken.", method: "Stringify the body; search for 'refreshToken'.", expected: "Not present." },
  "SU-06": { purpose: "Cookie options per spec 05 §6.1.", under: "signup Set-Cookie for refreshToken.", method: "Read the set-cookie header from SU-01; parse the refreshToken directive's attributes.", expected: "HttpOnly; SameSite=Lax; Max-Age=604800; Path=/. NOT Secure (local http)." },
  "SU-07": { purpose: "The id is a real Mongo ObjectId and the echoed fields match the request (email lowercased).", under: "signup body.user values.", method: "From SU-01, check body.user.", expected: "id matches /^[a-f0-9]{24}$/. username === request username. email === request email lowercased." },
  "SU-08": { purpose: "spec 05 §7 (username 3-20) + 03 §0 error shape.", under: "signup with username 'ab'.", method: "POST with username:'ab', valid email, valid password.", expected: "HTTP 400. Body matches { errors:[{field,message}] }. At least one entry field==='username'. No error key. No user/accessToken." },
  "SU-09": { purpose: "spec 05 §7 (valid email).", under: "signup with email 'nope'.", method: "POST with valid username, email:'nope', valid password.", expected: "HTTP 400; an entry field==='email'." },
  "SU-10": { purpose: "spec 05 §7 (password >= 8).", under: "signup with password 'short'.", method: "POST with valid username/email, password:'short'.", expected: "HTTP 400; an entry field==='password'." },
  "SU-11": { purpose: "spec 03 §0 — field must be username/email/password so the client maps it to the right input.", under: "signup with all three fields invalid.", method: "POST with username:'ab', email:'nope', password:'x'.", expected: "HTTP 400; every errors[].field is in {username,email,password}." },
  "SU-12": { purpose: "spec 05 §8 — normal path is the async validator (400 on the field); a race that slips past becomes a 409 (err.code===11000).", under: "signup with a duplicate username.", method: "Sign up a user, then POST /users/signup again with the SAME username (fresh email).", expected: "HTTP 400 with an errors[] entry field==='username' OR HTTP 409 with { error:<string> }. Either way: no password in the body; one of the two agreed shapes. (400 is expected with the async validator present; 409 is accepted as spec-compliant.)" },

  // ---- RF ----
  "RF-01": { purpose: "The normal 'nobody is logged in' case on start-up (spec 05 §6 step 1).", under: "POST /users/refresh with no cookie.", method: "POST /users/refresh with no Cookie header.", expected: "HTTP 401. Body matches { error:<string> } (GeneralError.strict())." },
  "RF-02": { purpose: "spec 06 §3.3 / 05 §8 — this 401 is answered in the route, not the central handler, and must not be logged as an error.", under: "backend/logs/error.log after a no-cookie /refresh.", method: "Record error.log byte length. Send the RF-01 request. Wait 300ms. Re-read the length; if it grew, check the appended text.", expected: "error.log did not grow, OR the appended text mentions neither /users/refresh nor [ERROR]." },
  "RF-03": { purpose: "spec 03 §1 / 05 §6 — a valid refresh returns user + a new access token.", under: "POST /users/refresh with a valid cookie.", method: "signupAndGetCookie(), then POST /users/refresh with Cookie: refreshToken=<value>.", expected: "HTTP 200. Body matches AuthSuccess.strict() — user is {id,username,email} + accessToken. No password/hash/'refreshToken' string in the body." },
  "RF-04": { purpose: "The core rotation property — a new refresh cookie is issued (spec 05 §4, 04 'RefreshToken').", under: "refresh Set-Cookie.", method: "From RF-03, read set-cookie.", expected: "A refreshToken cookie is set; its value differs from the one sent in. Same attributes (HttpOnly, SameSite=Lax, Max-Age=604800, Path=/)." },
  "RF-05": { purpose: "The previous refresh token stops working after one successful rotation (spec 05 §4 steps 3-4).", under: "reusing an old refresh cookie.", method: "signupAndGetCookie() -> C1. POST /refresh with C1 -> 200, yields C2. POST /refresh AGAIN with C1.", expected: "The second call with C1 returns HTTP 401 and clears the cookie. C2 still works (POST /refresh with C2 -> 200)." },
  "RF-06": { purpose: "spec 05 §6 /refresh step 5 — a tampered token is rejected and the cookie cleared.", under: "POST /users/refresh with a mutated JWT.", method: "signupAndGetCookie(), mutate the last char of the JWT, POST /refresh with the mutated cookie.", expected: "HTTP 401, body { error:<string> }. set-cookie clears refreshToken." },
  "RF-07": { purpose: "Rotation deletes the old row and inserts one new row (spec 04 'RefreshToken', 05 §4).", under: "refreshtokens collection count across a rotation.", method: "With a QA mongo connection: signupAndGetCookie(); record count N. POST /refresh once. Wait 200ms. Re-count -> M.", expected: "M === N (one deleted, one inserted). Exactly one row exists for this user after the rotation." },
  "RF-08": { purpose: "Refresh returns the correct user (spec 03 §1 — user + token).", under: "refresh body.user.", method: "Sign up user U (known username/email), get its cookie, POST /refresh.", expected: "body.user.username === U.username; body.user.email === U.email lowercased; body.user.id is a 24-hex string." },
  "RF-09": { purpose: "A token never issued by this server (not in the collection) is rejected at step 1 of userService.refresh (spec 05 §4).", under: "POST /users/refresh with a self-signed unknown token.", method: "Sign a valid JWT with REFRESH_TOKEN_SECRET for {userId:<random ObjectId>} WITHOUT saving a row; send it as the cookie.", expected: "HTTP 401, body { error:<string> }, cookie cleared." },

  // ---- LO ----
  "LO-01": { purpose: "spec 03 §1 / 05 §6 /logout — 204 with no body.", under: "POST /users/logout with a valid cookie.", method: "signupAndGetCookie(), then POST /users/logout with Cookie: refreshToken=<value>.", expected: "HTTP 204. Response body is empty ('')." },
  "LO-02": { purpose: "The cookie must actually be cleared server-side (spec 03 §1).", under: "logout Set-Cookie.", method: "Read set-cookie from LO-01.", expected: "A refreshToken directive that expires/empties it (empty value, or Expires in the past, or Max-Age=0)." },
  "LO-03": { purpose: "Logout really invalidates the token (spec 05 §6 /logout, 04 'RefreshToken': Logout deletes it).", under: "using an old cookie on /refresh after logout.", method: "signupAndGetCookie() -> C1. POST /logout with C1. Then POST /users/refresh with C1.", expected: "The /refresh call returns HTTP 401." },
  "LO-04": { purpose: "spec 05 §6 /logout — safe to call when nobody is logged in.", under: "POST /users/logout with no cookie.", method: "POST /users/logout with no Cookie header.", expected: "HTTP 204, empty body. No error." },
  "LO-05": { purpose: "userService.logout is idempotent (spec 05 §4 logout).", under: "POST /users/logout twice with the same cookie.", method: "signupAndGetCookie() -> C1. POST /logout with C1, then POST /logout with C1 again.", expected: "Both calls return HTTP 204. Neither throws a 500." },

  // ---- XC ----
  "XC-01": { purpose: "spec 03 §0 — the client understands only { error:string } or { errors:[{field,message}] }.", under: "error bodies from login(400/401), signup(400), refresh(401), 404.", method: "Collect those five error responses; parse JSON of each.", expected: "Each body has EITHER exactly key error (string) OR exactly key errors (array of {field,message}). No body has both or any third top-level key." },
  "XC-02": { purpose: "spec 03 §0 — the client puts the message under the input named by field.", under: "signup with username:'ab', email:'bad', password:'x'.", method: "POST that body; parse the 400.", expected: "HTTP 400; every errors[].field is one of 'username', 'email', 'password'." },
  "XC-03": { purpose: "spec 01 §2, 03 §1, 05 §5 — password never leaves the server.", under: "success bodies of signup(201), login(200), refresh(200).", method: "Capture the three success bodies; stringify each; search for 'password', '$2a$', '$2b$', '$2y$'.", expected: "None present in any of the three." },
  "XC-04": { purpose: "spec 05 §5 — refreshToken only in Set-Cookie, never in a JSON body.", under: "signup + login bodies and their Set-Cookie.", method: "Stringify each body; search for 'refreshToken'. Confirm set-cookie carries it.", expected: "'refreshToken' absent from every body; present in set-cookie on signup/login." },
  "XC-05": { purpose: "spec 07 §2 #1 — helmet is mounted so it covers every response.", under: "response headers on a 201 signup.", method: "POST a fresh signup; inspect response headers.", expected: "x-content-type-options: nosniff; x-dns-prefetch-control present; content-security-policy present; x-powered-by absent." },
  "XC-06": { purpose: "spec 07 §2 #1 — 'including errors'.", under: "response headers on a 404.", method: "GET /nope; inspect headers.", expected: "x-content-type-options: nosniff present; x-powered-by absent." },
  "XC-07": { purpose: "spec 03 §0 / 07 §2 #2 — cors({ origin:'http://localhost:5173', credentials:true }), not *.", under: "CORS response headers for the client origin.", method: "POST /users/login with request header Origin: http://localhost:5173.", expected: "access-control-allow-origin: http://localhost:5173 (exact); access-control-allow-credentials: true." },
  "XC-08": { purpose: "The browser preflight must succeed for the client origin.", under: "OPTIONS /users/login.", method: "OPTIONS /users/login with Origin: http://localhost:5173 and Access-Control-Request-Method: POST.", expected: "A 2xx/204 status; access-control-allow-origin: http://localhost:5173; access-control-allow-credentials: true." },
  "XC-09": { purpose: "spec 07 §27 — CORS locked to one origin.", under: "CORS with a foreign origin.", method: "POST /users/login with Origin: http://evil.example.", expected: "The response does NOT carry access-control-allow-origin: http://evil.example." },
  "XC-10": { purpose: "spec 07 §4 #1 — an unmatched path returns 404 with the general error shape.", under: "GET /this/does/not/exist.", method: "GET a random unmatched path.", expected: "HTTP 404; body has exactly one key error (a string)." },
  "XC-11": { purpose: "spec 07 §4 #1 fixes the 404 body as { \"error\": \"Not found.\" }.", under: "GET an unmatched path — body message.", method: "GET a random unmatched path; read body.error.", expected: "Per spec: body.error === 'Not found.'. Actual code (index.js line 57): { error: 'Not Found' } (capital F, no period).", divergence: "SPEC DIVERGENCE — 404 message string. Status (404) and shape ({error}) are correct. Fix: change index.js line 57 to 'Not found.', or amend spec 07 §4. Low severity (client shows a generic message)." },
  "XC-12": { purpose: "spec 07 §2 #3-#4 — express.json before the routers, cookie-parser before /users/refresh.", under: "JSON body parsing + cookie parsing wiring.", method: "(a) POST /users/login with a JSON body -> the server reads req.body. (b) POST /users/refresh with Cookie: refreshToken=x -> the server reads req.cookies.", expected: "(a) 400 with the errors shape. (b) 401 with the error shape — never a 500/TypeError." },
  "XC-13": { purpose: "spec 07 §2 lists helmet() as middleware #1; the code mounts compression() first (line 30) and helmet() second (line 31).", under: "index.js app.use order + helmet coverage.", method: "Static: record the order of app.use( lines. Behavioural: confirm helmet headers on a 404.", expected: "helmet headers present on all responses (PASS). The ordering difference is recorded as a design note.", divergence: "DESIGN NOTE — compression() is mounted before helmet(); spec 07 §2 says helmet is #1. compression() sends no response of its own, so nothing is left un-helmeted. Not a functional failure. Fix: reorder in index.js, or note the exception in spec 07 §2." },
  "XC-14": { purpose: "spec 07 §2 #6 — a loose blanket limiter distinct from the strict /users/login one.", under: "the global rate limiter config + light behaviour.", method: "Static: index.js constructs rateLimit({ windowMs:15*60*1000, max:300, ... }) before the routers. Behavioural: send 15 rapid GET / requests.", expected: "Static config found with max:300; the 15 requests all return a non-429 status." },

  // ---- LI ----
  "LI-01": { purpose: "Valid credentials log in (spec 03 §1: 200).", under: "POST /users/login happy path.", method: "beforeAll restarts the server (clean rate limiter) and signs up a known user. POST /users/login with that user's {username, password}.", expected: "HTTP 200." },
  "LI-02": { purpose: "Login's success body equals signup's shape (spec 05 §2).", under: "login success body.", method: "Validate the LI-01 body against AuthSuccess.strict().", expected: "Passes; keys exactly user ({id,username,email}) + accessToken." },
  "LI-03": { purpose: "Login sets a fresh HttpOnly refresh cookie (spec 05 §6.1).", under: "login Set-Cookie.", method: "Read set-cookie from a successful login.", expected: "refreshToken cookie with HttpOnly, SameSite=Lax, Max-Age=604800, Path=/." },
  "LI-04": { purpose: "spec 03 §1 / 05 §5 — no secret in the body.", under: "login body — secret leak.", method: "Stringify the LI-01 body; search for 'password', '$2a$'/'$2b$'/'$2y$', 'refreshToken'.", expected: "None present." },
  "LI-05": { purpose: "spec 05 §7 — username present on /login.", under: "POST /users/login with only { password }.", method: "POST { password:'password123' } only.", expected: "HTTP 400; body matches ValidationError; an entry field==='username'; no error key." },
  "LI-06": { purpose: "spec 05 §7 — password present on /login.", under: "POST /users/login with only { username }.", method: "POST { username:'whoever' } only.", expected: "HTTP 400; an entry field==='password'." },
  "LI-07": { purpose: "spec 03 §1 / 05 §4/§8 — the response must not reveal which credential was wrong.", under: "wrong password vs unknown username.", method: "Case A: known username + wrong password. Case B: unknown username + any password. Compare status and body.", expected: "Both HTTP 401. Both bodies are exactly { error:'Username or password is incorrect.' } and deep-equal to each other. Neither sets a refreshToken cookie." },
  "LI-08": { purpose: "spec 03 §1 'Rate-limit this route more strictly'; 05 §6 (5 / 15 min).", under: "the 6th /users/login attempt in the window.", method: "restartServer() for a clean limiter; sign up a fresh user; send 5 failing POST /users/login (wrong password), then a 6th.", expected: "Attempts 1-5 return 401. Attempt 6 returns 429 with a { error:<string> } body." },
  "LI-09": { purpose: "A 429 from the login limiter uses the general error shape (spec 03 §0).", under: "the limiter's 429 body.", method: "With the limiter still tripped from LI-08, POST /users/login once more.", expected: "HTTP 429; body validates against GeneralError.strict() — exactly one key error, a string. No errors array." },
  "LI-10": { purpose: "FINDING F-1 (not a spec assertion): two logins by the same user within one wall-clock second — or a signup immediately followed by a login — make issueTokenPair sign an IDENTICAL refresh-token JWT (payload is just { userId }, jwt `iat` is second-resolution), which collides on the RefreshToken.token unique index; errorHandler maps the E11000 to a 409. A user who double-submits the login form gets a 409.", under: "POST /users/login twice in quick succession for one user.", method: "makeUser(); signup(u); POST /users/login twice back-to-back with the same credentials.", expected: "At least one of the two responses is HTTP 409, and that 409 body is the agreed { error:<string> } shape. Test PASSES by confirming the current behaviour so the finding is visible; the fix is a jti/nonce in the refresh payload (or a documented spec note).", divergence: "FINDING F-1 — same-second re-login / signup-then-login collides on RefreshToken.token unique index -> 409. Not covered by any spec assertion; a real UX bug (double-click login). Fix: add a random jti to the refresh-token payload in issueTokenPair so two tokens are never byte-identical, OR document the constraint in spec 05 §4." },

  // ---- UJ (e2e, real browser + real server) ----
  "UJ-01": { purpose: "Proves the cookie/token wiring end to end -- the access token lives only in memory, a reload loses it but the refreshToken cookie silently restores the session, and logout actually ends the session server-side (not just a client redirect).", under: "SignupPage -> POST /users/signup; App's mount-time restoreSession() -> POST /users/refresh; NavBar logged-in state and logout -> POST /users/logout.", method: "makeUser(); fill and submit the signup form; wait for /home; assert NavBar shows the username and no Log-in link; dump window.localStorage and assert no key/value contains 'token'; reload the page; assert still on /home, still logged in; click Log out; assert redirected to /login; navigate straight to /home again.", expected: "Signup lands on /home (not an error). localStorage never contains a token. After reload, still on /home with the username visible (proves restoreSession() used the cookie). After logout, /login is shown, and a direct visit to /home bounces back to /login (proves the server-side session is actually gone, not just the client state)." },
  "UJ-02": { purpose: "Mirrors LI-07 at the browser layer: the UI must render the one generic message, not something more specific, and a failed attempt must not block a legitimate retry.", under: "LoginPage -> POST /users/login; the role=alert error banner; retry with correct credentials.", method: "signup() a user directly via the API; go to /login; submit the known username with a wrong password; read the role=alert banner text; clear and refill the password with the correct one; submit again.", expected: "The banner text is exactly 'Username or password is incorrect.' -- the same generic sentence for any wrong credential. The second, correct submission lands on /home -- the earlier failure left no stuck state (form, store, or rate limiter) blocking it." },

  // ---- CS ----
  "CS-01": { purpose: "Free text like \"fuel\" must find a category named \"Fuel\".", under: "findByName().", method: "getCategoriesByUser -> [{_id:'c1',name:'Fuel',parent:null}]. findByName('u1','fuel').", expected: "Returns the one match." },
  "CS-02": { purpose: "The chat's category search can be narrowed to subcategories of one main.", under: "findByName() with a parentId.", method: "Two categories named 'Coffee' under different parents p1/p2. findByName('u1','coffee','p1').", expected: "Returns only the p1 match." },
  "CS-03": { purpose: "The caller (route / resolveCategory) decides behaviour from the match count.", under: "findByName() cardinality.", method: "getCategoriesByUser resolves 0, then 1, then 2 same-named categories across three calls.", expected: "findByName resolves arrays of length 0, 1, 2 respectively." },
  "CS-04": { purpose: "Happy path, parent defaults to null.", under: "createCategory() with no parent.", method: "getCategoriesByUser -> []. createCategory('u1',{name:'Pets'}).", expected: "categoryRepository.createCategory called once with {name:'Pets', parent:null, owner:'u1'}." },
  "CS-05": { purpose: "spec §2 step 1 -- a nonexistent parent.", under: "createCategory() parent validation.", method: "getCategoriesByUser -> []. createCategory('u1',{name:'Pets',parent:'ghost'}).", expected: "Rejects {status:400, message:'No such category to add a subcategory to.'}. createCategory never called.", divergence: "CORRECTED 2026-09-16 -- was mocking findCategoryById, matching an old by-id parent resolution. Bug 9's fix made createCategory resolve parent by NAME via findByName/getCategoriesByUser; the stale mock made this test throw a TypeError instead of exercising the real branch until this pass's full run caught it." },
  "CS-06": { purpose: "The multi-match branch of createCategory's OWN parent resolution (distinct from resolveCategory's equivalent for expenses, CR-04) -- untested at any level before this pass.", under: "createCategory() with an ambiguous parent name.", method: "getCategoriesByUser -> two categories both named 'p1'. createCategory('u1',{name:'Pets',parent:'p1'}).", expected: "Rejects {status:400, message:'More than one category named \"p1\".'}.", divergence: "REPLACES the old CS-06 ('parent owned by another user'), a scenario the current code can no longer reach -- findByName's search is already scoped to getCategoriesByUser(userId), so a foreign-owned same-named category is never a candidate." },
  "CS-07": { purpose: "Two-level rejection.", under: "createCategory() with a parent that is itself a subcategory.", method: "getCategoriesByUser -> [{_id:'p1', name:'p1', owner:'u1', parent:'grandparent'}]. createCategory('u1',{name:'X',parent:'p1'}).", expected: "Rejects {status:400, message:'Categories can only be two levels deep.'}.", divergence: "CORRECTED 2026-09-16, same reasoning as CS-05." },
  "CS-08": { purpose: "spec §2 step 2 -- matches the compound unique index.", under: "createCategory() duplicate name.", method: "getCategoriesByUser -> [{_id:'c1',name:'Pets',parent:null}]. createCategory('u1',{name:'Pets'}).", expected: "Rejects {status:409, message:'A category named \"Pets\" already exists here.'}. createCategory never called." },
  "CS-09": { purpose: "spec §2 updateCategory step 1.", under: "updateCategory() missing/foreign category.", method: "findCategoryById -> null, then -> {owner:'someone-else'}.", expected: "Both reject {status:404, message:'Category not found.'}." },
  "CS-10": { purpose: "\"Other\" cannot be renamed.", under: "updateCategory() on isProtected.", method: "findCategoryById -> {owner:'u1', isProtected:true}. updateCategory('u1','c1',{name:'Renamed'}).", expected: "Rejects {status:400, message:'\"Other\" cannot be renamed.'}." },
  "CS-11": { purpose: "A genuine collision with a different sibling.", under: "updateCategory() duplicate name (a different doc).", method: "findCategoryById -> non-protected c1. getCategoriesByUser -> [c1, c2] both named 'Pets'. updateCategory('u1','c1',{name:'Pets'}).", expected: "Rejects {status:409, message:'A category named \"Pets\" already exists here.'} -- the match against c2 trips it." },
  "CS-12": { purpose: "The one behaviour that differs from createCategory's duplicate check -- renaming to its own current name is not a duplicate.", under: "updateCategory() self-exclusion.", method: "findCategoryById -> c1. getCategoriesByUser -> [c1] only. updateCategory('u1','c1',{name:'Pets'}).", expected: "Resolves. categoryRepository.updateCategory called with ('c1', {name:'Pets'})." },
  "CS-13": { purpose: "spec §2 deleteCategory steps 1-3.", under: "deleteCategory() error branches + happy path.", method: "(a) findCategoryById->null (b) ->{owner:'other'} (c) ->{owner:'u1',isProtected:true} (d) ->{owner:'u1',isProtected:false}, deleteCategory resolves.", expected: "(a)/(b) 404 'Category not found.'; (c) 400 '\"Other\" cannot be deleted.'; (d) resolves, deleteCategory called with the id." },

  // ---- ES ----
  "ES-01": { purpose: "The model never invents a category.", under: "resolveCategory (via createExpense) zero matches.", method: "categoryService.findByName -> []. createExpense('u1',{amount:10,category:'Ghost'}).", expected: "Rejects {status:400, message:'No category named \"Ghost\". Create it first?'}. createExpense (repo) never called." },
  "ES-02": { purpose: "Two different mains can each have a same-named subcategory (confirmed in scope by 09 §8).", under: "resolveCategory more than one match.", method: "findByName resolves two docs named 'Coffee'. createExpense('u1',{amount:10,category:'Coffee'}).", expected: "Rejects {status:400, message:'More than one category matches \"Coffee\".', candidates:[the two docs]}." },
  "ES-03": { purpose: "spec §3 createExpense steps 1-2.", under: "createExpense() category resolution before save.", method: "findByName -> [{_id:'cat1'}]. createExpense repo resolves. createExpense('u1',{amount:12,store:'Aroma',category:'Coffee'}).", expected: "expenseRepository.createExpense called with {amount:12,store:'Aroma',description:undefined,date:undefined,category:'cat1',user:'u1'}." },
  "ES-04": { purpose: "Ownership check via findExpenseById, not a second query.", under: "editExpense() missing/foreign expense.", method: "findExpenseById -> null, then -> owned by 'someone-else'.", expected: "Both reject {status:404, message:'Expense not found.'}. updateExpense never called." },
  "ES-05": { purpose: "Moving an expense to a different category is an ordinary edit whose changes.category arrives as a name.", under: "editExpense() resolves changes.category.", method: "findExpenseById -> owned. findByName -> [{_id:'cat2'}]. editExpense('u1','e1',{category:'Entertainment'}).", expected: "updateExpense called with ('e1', {category:'cat2'})." },
  "ES-06": { purpose: "An edit that does not move the expense skips category resolution entirely.", under: "editExpense() with no changes.category.", method: "findExpenseById -> owned. editExpense('u1','e1',{amount:30}).", expected: "categoryService.findByName never called. updateExpense called with ('e1', {amount:30})." },
  "ES-07": { purpose: "Same ownership check as editExpense.", under: "deleteExpense() missing/foreign expense.", method: "findExpenseById -> null, then -> owned by 'someone-else'.", expected: "Both reject {status:404, message:'Expense not found.'}." },
  "ES-08": { purpose: "The owned/found case.", under: "deleteExpense() happy path.", method: "findExpenseById -> owned e1. deleteExpense('u1','e1').", expected: "expenseRepository.deleteExpense called with 'e1'." },
  "ES-09": { purpose: "Used by the dashboard and the chat's edit/delete search step alike, with no logic of its own.", under: "getForUser() passthrough.", method: "queryExpenses -> []. getForUser('u1',{from:'2026-01-01',text:'coffee'}).", expected: "queryExpenses called with ('u1', {from:'2026-01-01',text:'coffee'}) unchanged." },

  // ---- AI ----
  "AI-01": { purpose: "The model must see the user's real data, not a stale or empty list; a subcategory's parent id is resolved to its main category's NAME before embedding.", under: "buildPrompt() embeds categories (parent-resolved)/recentExpenses (verbatim).", method: "buildPrompt([{name:'Fuel'}], [{amount:12,store:'Aroma'}]).", expected: "Returned string contains the expenses array verbatim, and categories as [{name:'Fuel',parent:null}] -- not the raw input shape.", divergence: "CORRECTED 2026-09-16 -- buildPrompt grew a withParentNames transform (same session as the chat-flow fixes) that this test's old verbatim-categories assertion didn't account for; caught by this pass's full-suite run." },
  "AI-02": { purpose: "A first-time user with no categories/expenses yet must not confuse the model with the string 'undefined'.", under: "buildPrompt() defaults.", method: "buildPrompt(undefined, null).", expected: "No 'undefined' substring anywhere. The categories block and the expenses block each trim to exactly '[]'." },
  "AI-03": { purpose: "A guard against silently dropping an intent from the instructions while schema.js's enum still lists it.", under: "buildPrompt() intent coverage.", method: "buildPrompt([],[]); check every INTENTS value appears.", expected: "All 8 values appear in the text." },
  "AI-04": { purpose: "The two files can never drift apart.", under: "INTENTS shared reference.", method: "Read prompt.js's INTENTS and schema.js's responseSchema.properties.intent.enum.", expected: "INTENTS is exactly the 8 values. responseSchema...enum === INTENTS by reference equality." },
  "AI-05": { purpose: "Which fields matter depends on the intent -- nothing else is force-required.", under: "responseSchema.required.", method: "Read responseSchema.required.", expected: "Deep-equals ['intent','reply']." },
  "AI-06": { purpose: "Proves the wiring, not just the return value.", under: "parseMessage() call args to generateContent.", method: "Fake client resolving {text:'{\"intent\":\"unknown\",\"reply\":\"ok\"}'}. parseMessage('hi',[{name:'Fuel'}],[],{client}).", expected: "generateContent called once with model 'gemini-flash-lite-latest', contents:'hi', config.systemInstruction===buildPrompt(...), config.responseMimeType:'application/json', config.responseSchema===responseSchema (by reference)." },
  "AI-07": { purpose: "The function is a thin pass-through of the model's structured output.", under: "parseMessage() happy path.", method: "Fake client resolves a JSON create-expense payload.", expected: "Resolves the parsed object deep-equal to the input JSON." },
  "AI-08": { purpose: "spec §8 'It never throws a raw provider error at the route.'", under: "parseMessage() generateContent throw.", method: "Fake client's generateContent rejects with Error('ECONNRESET...').", expected: "Rejects with exactly {status:502, message:'Could not reach the AI right now. Please try again.'} -- not the original error." },
  "AI-09": { purpose: "The SDK's responseSchema is not itself enforced by JSON.parse.", under: "parseMessage() unparseable response.text.", method: "Fake client resolves {text:'not json {{{'}.", expected: "Rejects with the identical 502 shape as AI-08." },
  "AI-10": { purpose: "Tests (and a cold server) never need a real GEMINI_API_KEY until the first real call, and repeated calls reuse one client.", under: "getDefaultClient() singleton.", method: "Stub @google/genai's GoogleGenAI export with a spy constructor (keeping the real Type export for schema.js). Call parseMessage twice with no options.client.", expected: "The stub constructor called exactly once, with {apiKey: process.env.GEMINI_API_KEY}." },
  "AI-11": { purpose: "Every other test (and Adam's own) must never risk touching the real SDK.", under: "getDefaultClient() not built when a client is injected.", method: "Same stub as AI-10. Call parseMessage with an explicit fake client.", expected: "The stub GoogleGenAI constructor is never called." },
  "AI-12": { purpose: "Nothing outside backend/ai/ may import prompt.js/schema.js directly.", under: "backend/ai/index.js exports.", method: "require index.js; inspect its keys.", expected: "Exported keys are exactly ['parseMessage']." },

  // ---- CH ----
  "CH-01": { purpose: "isProtected must only ever be true on the one real \"Other\" category.", under: "pre('save') isProtected guard.", method: "new Category({name:'Fake Other', owner, isProtected:true}).save().", expected: "Rejects, error mentions \"protected\"." },
  "CH-02": { purpose: "A subcategory's parent must actually exist.", under: "pre('save') parent-exists guard.", method: "new Category({name:'Sub', owner, parent:<fresh ObjectId>}).save().", expected: "Rejects, error mentions 'Parent category does not exist'." },
  "CH-03": { purpose: "Blocks a third level of nesting.", under: "pre('save') two-level guard.", method: "Seed main A, sub B under A. new Category({name:'C',owner,parent:B._id}).save().", expected: "Rejects, error mentions 'cannot have a parent that is itself a subcategory'." },
  "CH-04": { purpose: "The success path must actually succeed.", under: "pre('save') valid main category.", method: "new Category({name:'Main', owner}).save().", expected: "Resolves; parent unset." },
  "CH-05": { purpose: "The success path for a subcategory must actually succeed.", under: "pre('save') valid subcategory.", method: "Seed main A. new Category({name:'Sub',owner,parent:A._id}).save().", expected: "Resolves; parent===A._id." },
  "CH-06": { purpose: "\"Other\" cannot be renamed.", under: "pre('findOneAndUpdate') protected-rename guard.", method: "Seed Other. findOneAndUpdate({_id:other._id},{name:'Renamed'}).", expected: "Rejects, error mentions 'cannot be renamed'." },
  "CH-07": { purpose: "\"Other\" cannot be unprotected.", under: "pre('findOneAndUpdate') unprotect guard.", method: "Seed Other. findOneAndUpdate({_id:other._id},{isProtected:false}).", expected: "Rejects, error mentions 'cannot be unprotected'." },
  "CH-08": { purpose: "A new parent must actually exist.", under: "pre('findOneAndUpdate') parent-exists guard.", method: "Seed a plain category. findOneAndUpdate({_id},{parent:<fresh ObjectId>}).", expected: "Rejects, error mentions 'Parent category does not exist'." },
  "CH-09": { purpose: "A new parent cannot itself be a subcategory.", under: "pre('findOneAndUpdate') two-level guard.", method: "Seed main A, sub B under A, plain C. findOneAndUpdate({_id:C._id},{parent:B._id}).", expected: "Rejects, error mentions 'cannot have a parent that is itself a subcategory'." },
  "CH-10": { purpose: "A normal rename must actually succeed.", under: "pre('findOneAndUpdate') success path.", method: "Seed a plain category. findOneAndUpdate({_id},{name:'New Name'},{new:true}).", expected: "Resolves with name==='New Name'." },
  "CH-11": { purpose: "\"Other\" cannot be deleted.", under: "pre('findOneAndDelete') protected guard.", method: "Seed Other. findOneAndDelete({_id:other._id}).", expected: "Rejects, error mentions 'cannot be deleted'." },
  "CH-12": { purpose: "Spending history is never lost when a subcategory is removed.", under: "pre('findOneAndDelete') cascade for a subcategory.", method: "Seed Other, main A, sub B under A, one expense on B. findOneAndDelete({_id:B._id}).", expected: "The expense's category becomes Other._id; B is gone." },
  "CH-13": { purpose: "Deleting a main category must not orphan its subs' spending history.", under: "pre('findOneAndDelete') cascade for a main + its subs.", method: "Seed Other, main A, subs B1/B2, one expense on each of A/B1/B2. findOneAndDelete({_id:A._id}).", expected: "All three expenses' category becomes Other._id; A, B1, B2 are all gone." },
  "CH-14": { purpose: "A bulk delete must not silently remove a protected category.", under: "pre('deleteMany') protected guard.", method: "Seed Other + a plain category. deleteMany({_id:{$in:[other._id,plain._id]}}).", expected: "Rejects, error mentions 'Protected categories cannot be deleted'. Both docs still exist." },
  "CH-15": { purpose: "A bulk delete of only unprotected categories must succeed.", under: "pre('deleteMany') success path.", method: "Seed Other + two plain categories. deleteMany({owner,isProtected:false}).", expected: "Resolves; the two plain categories are gone; Other remains." },
  "CH-16": { purpose: "resetToDefaults's single-owner reset must be let through even though it matches \"Other\".", under: "pre('deleteMany') single-key {owner} exception.", method: "Seed Other + two plain categories. deleteMany({owner}).", expected: "Resolves; all three (including Other) are gone." },

  // ---- CI ----
  "CI-01": { purpose: "The default set has the right shape.", under: "seedDefaultCategories() mains + sub counts.", method: "categoryService.seedDefaultCategories(userId).", expected: "17 docs total; 8 with no parent; Food/Transport/Home have exactly their 3 named subs; the other 5 mains have none." },
  "CI-02": { purpose: "Only \"Other\" is protected.", under: "seedDefaultCategories() isProtected flag.", method: "Same call as CI-01; inspect 'Other' and 'Food'.", expected: "'Other'.isProtected===true; 'Food'.isProtected===false." },
  "CI-03": { purpose: "Nothing is left pointing at a category about to be removed.", under: "resetToDefaults() reassignment ordering.", method: "Seed defaults; create expenses on the seeded 'Food' and 'Other'. resetToDefaults(userId).", expected: "A fresh default set is returned. Both expenses now point at the NEW Other._id (different from the old). Category.findById(oldOther) is null." },
  "CI-04": { purpose: "No orphaned category ids after a reset.", under: "resetToDefaults() final state.", method: "seedDefaultCategories then resetToDefaults. Category.find({owner:userId}).", expected: "Exactly 17 documents, same shape as a fresh seedDefaultCategories." },

  // ---- EI ----
  "EI-01": { purpose: "The happy path saves everything.", under: "createManyExpenses() all drafts resolve.", method: "Seed category 'Food'. createManyExpenses(userId, [{amount:10,category:'Food'},{amount:20,category:'Food'}]).", expected: "Resolves 2 saved docs; Expense.countDocuments({user:userId})===2." },
  "EI-02": { purpose: "One bad category name must fail the whole batch -- no partial save.", under: "createManyExpenses() resolve-all-first ordering.", method: "Seed category 'Food'. createManyExpenses(userId, [{amount:10,category:'Food'},{amount:20,category:'Ghost'}]).", expected: "Rejects {status:400, message:'No category named \"Ghost\". Create it first?'}. Expense.countDocuments({user:userId})===0 -- the first (valid) draft was never written." },

  // ---- DL ----
  "DL-01": { purpose: "The reassignment middleware's lookup must find the right doc.", under: "findOtherCategory().", method: "Seed {name:'Other',owner,parent:null}. findOtherCategory(owner), then findOtherCategory(<a stranger>).", expected: "First resolves the doc; second resolves null." },
  "DL-02": { purpose: "Bulk insert must round-trip real ids, including cross-batch parent refs.", under: "createManyCategories().", method: "Insert two mains A/B; then insert a sub referencing A._id as parent.", expected: "Both calls resolve real docs with _id; the sub's parent equals A._id." },
  "DL-03": { purpose: "A user-scoped bulk delete must not touch another user's categories.", under: "deleteCategoriesByUser().", method: "Seed 2 categories for user X, 1 for user Y. deleteCategoriesByUser(X).", expected: "Category.countDocuments({owner:X})===0; {owner:Y}===1." },
  "DL-04": { purpose: "The aggregation must total correctly per category.", under: "getExpenseTotalByCategory().", method: "2 expenses on category A (10,15), 1 on B (7), one user. getExpenseTotalByCategory(userId).", expected: "Rows equivalent to {_id:A,total:25} and {_id:B,total:7}." },
  "DL-05": { purpose: "The date-range narrows the aggregation, not just the read.", under: "getExpenseTotalByCategory() with from/to.", method: "One expense Jan 1 (10), one Feb 1 (20), same category. getExpenseTotalByCategory(userId, Feb1, Feb28).", expected: "One row totalling 20 -- January excluded." },
  "DL-06": { purpose: "A user with nothing recorded must not error.", under: "getExpenseTotalByCategory() empty case.", method: "getExpenseTotalByCategory(<a fresh, never-used userId>).", expected: "Resolves []." },
  "DL-07": { purpose: "The ownership-check lookup used by editExpense/deleteExpense.", under: "findExpenseById().", method: "Create one expense; findExpenseById(its id), then findExpenseById(<fresh ObjectId>).", expected: "First resolves the doc; second resolves null." },
  "DL-08": { purpose: "resetToDefaults's reassignment step must move every one of a user's expenses, and only that user's.", under: "reassignExpensesToCategory().", method: "2 expenses for user X on category A, 1 for user Y on A. reassignExpensesToCategory(X, B).", expected: "Both of X's expenses now on B; Y's is untouched (still A)." },
  "DL-09": { purpose: "The chat's edit/delete free-text match must be forgiving (case, partial).", under: "queryExpenses() text filter.", method: "Expenses with store:'Aroma Coffee', description:'team lunch', store:'Hardware Store'. queryExpenses({text:'coffee'}), then ({text:'LUNCH'}).", expected: "First returns only 'Aroma Coffee'; second returns only 'team lunch' -- case-insensitive, partial, matches either field." },
  "DL-10": { purpose: "The dashboard's period filter and the chat's free-text match must compose, not override each other.", under: "queryExpenses() text + date range combined.", method: "(a) 'Coffee Shop' Jan 5, (b) 'Coffee Shop' Mar 5, (c) 'Other Store' Jan 5. queryExpenses({text:'coffee', from:Jan1, to:Jan31}).", expected: "Only (a) -- (b) excluded by date, (c) by text." },

  // ---- CC ----
  "CC-01": { purpose: "The chat's write step must be behind auth like everything else.", under: "POST /chat/confirm with no Authorization.", method: "POST /chat/confirm {intent:'create-expense', drafts:[...]} with no header.", expected: "401. No expense is created." },
  "CC-02": { purpose: "The single-draft create path -- every stored field correct, not just amount.", under: "POST /chat/confirm create-expense (one draft).", method: "Sign up (seeds defaults incl. 'Food'). POST /chat/confirm {intent:'create-expense', drafts:[{amount:12.5,store:'Aroma',category:'Food'}]}.", expected: "200 {changed:<saved expense>}; changed.amount===12.5, changed.store==='Aroma', changed.category equals Food's real _id (not the string 'Food'), changed.date is today. GET /expenses includes it.", divergence: "STRENGTHENED 2026-09-16 -- Finding A (2026-09-16-manual-bugs-found.md) named this exact test as only checking amount and row existence, never store/category/date. Now checks all four." },
  "CC-03": { purpose: "spec §4 -- drafts.length>1 uses the batch service call, not N single calls -- every stored field correct on both.", under: "POST /chat/confirm create-expense (two drafts).", method: "Same user; two drafts of different amounts on 'Food'.", expected: "200 {changed:[<2 saved expenses>]}; amounts match by value ([1,2], not just count===2); both resolve to Food's real _id. GET /expenses shows both.", divergence: "STRENGTHENED 2026-09-16, same reasoning as CC-02." },
  "CC-04": { purpose: "The one-field edit path.", under: "POST /chat/confirm edit-expense.", method: "Create an expense; POST /chat/confirm {intent:'edit-expense', id, changes:{amount:99}}.", expected: "200 {changed:{...amount:99...}}." },
  "CC-05": { purpose: "Moving an expense to a different category is an ordinary edit whose changes.category is a name.", under: "POST /chat/confirm edit-expense with changes.category.", method: "Expense on 'Food'. POST /chat/confirm {intent:'edit-expense', id, changes:{category:'Transport'}}.", expected: "200; changed.category is Transport's real id, not the string 'Transport'." },
  "CC-06": { purpose: "The delete path.", under: "POST /chat/confirm delete-expense.", method: "Create an expense; POST /chat/confirm {intent:'delete-expense', id}.", expected: "200; a follow-up GET /expenses no longer includes it." },
  "CC-07": { purpose: "Every DAL lookup is scoped by userId first (spec 01 §6).", under: "POST /chat/confirm on someone else's expense id.", method: "User A creates an expense. User B calls POST /chat/confirm {intent:'delete-expense', id:<A's id>}.", expected: "404 {error:'Expense not found.'}. A's expense untouched." },
  "CC-08": { purpose: "spec §4 -- create-category should succeed and return the saved doc.", under: "POST /chat/confirm create-category.", method: "POST /chat/confirm {intent:'create-category', draft:{name:'Pets'}}.", expected: "200 { changed: <saved category> }; changed.name === 'Pets' with a real _id; GET /categories then includes it. FIXED (was 500 -- categoryModel.js pre('save') hook bug, see the CH-* findings; now passes)." },
  "CC-09": { purpose: "spec §4 -- edit-category should succeed.", under: "POST /chat/confirm edit-category.", method: "POST /chat/confirm {intent:'edit-category', id:<a default category>, changes:{name:'Renamed'}}.", expected: "200 { changed: {...name:'Renamed'...} }. FIXED (was 500, pre('findOneAndUpdate') hook bug; now passes)." },
  "CC-10": { purpose: "spec §4 -- delete-category should succeed and reassign its expenses to Other.", under: "POST /chat/confirm delete-category.", method: "Create an expense under 'Food'. POST /chat/confirm {intent:'delete-category', id:<Food's id>}.", expected: "200; 'Food' no longer in GET /categories; the earlier expense's category is now 'Other's id. FIXED (was 500, pre('findOneAndDelete') hook bug; now passes)." },
  "CC-11": { purpose: "spec §4 -- reset-categories should succeed, return a fresh default set, and land existing expenses on the NEW Other.", under: "POST /chat/confirm reset-categories.", method: "Create an expense under 'Food'. POST /chat/confirm {intent:'reset-categories'}.", expected: "200 { changed: [<fresh default categories>] }, non-empty, includes a new 'Other'; the earlier expense's category is the NEW 'Other's id, not the deleted one's. FIXED (was 500, pre('deleteMany') hook bug inside resetToDefaults; the reorder fix -- delete old categories, THEN reseed, THEN reassign to the new Other -- is what CC-11 proves; now passes)." },
  "CC-12": { purpose: "Documents a real gap: neither chat route validates req.body.intent against the seven known values.", under: "POST /chat/confirm with an unrecognized intent.", method: "POST /chat/confirm {intent:'not-a-real-intent'}.", expected: "200; the switch falls through every case, changed is never assigned, so the response has no usable 'changed' value. Not a QA-owned fix -- routes/chatRoute.js is Adam's file." },

  // ---- CM ----
  "CM-01": { purpose: "Chat history is private, like everything else behind requireAuth.", under: "GET /chat/messages with no Authorization.", method: "GET /chat/messages, no header.", expected: "401." },
  "CM-02": { purpose: "requireAuth runs before the handler -- an unauthenticated POST never reaches the AI or writes a Message.", under: "POST /chat/messages with no Authorization.", method: "POST /chat/messages {text:'spent 5'}, no header.", expected: "401. Message.countDocuments({}) unchanged." },
  "CM-03": { purpose: "spec §5 -- history loads oldest-first even though the route fetches newest-first internally.", under: "GET /chat/messages ordering.", method: "Seed 3 Message docs directly, createdAt strictly increasing (\"m1\",\"m2\",\"m3\"). GET /chat/messages.", expected: "text values are exactly ['m1','m2','m3']." },
  "CM-04": { purpose: "The documented default limit (50) doesn't truncate a normal history.", under: "GET /chat/messages with no limit param.", method: "Seed 5 messages. GET /chat/messages (no limit).", expected: "All 5 returned, oldest-first." },
  "CM-05": { purpose: "Off-by-one guard: fetch-newest-then-reverse must keep the right N, in the right order.", under: "GET /chat/messages?limit=.", method: "Seed 'm1'..'m5' in order. GET /chat/messages?limit=2.", expected: "Exactly ['m4','m5'], in that order." },
  "CM-06": { purpose: "spec §9 -- every DAL lookup is scoped by the logged-in user.", under: "GET /chat/messages scoping.", method: "Seed 2 messages for user A, 3 for user B. GET /chat/messages as A.", expected: "Exactly 2 results, both authored by A." },

  // ---- ER ----
  "ER-01": { purpose: "Category list is private.", under: "GET /categories with no Authorization.", method: "GET /categories, no header.", expected: "401." },
  "ER-02": { purpose: "spec §5 -- category list scoped to the logged-in user.", under: "GET /categories scoping.", method: "Sign up two users (each gets 17 default categories). GET /categories as A, then as B.", expected: "A gets exactly 17; A's and B's id sets are disjoint." },
  "ER-03": { purpose: "Expense list is private.", under: "GET /expenses with no Authorization.", method: "GET /expenses, no header.", expected: "401." },
  "ER-04": { purpose: "The date-range filter actually narrows.", under: "GET /expenses?from=&to=.", method: "One expense dated today (via chat/confirm). GET /expenses?from=<tomorrow>&to=<tomorrow+1>.", expected: "200 [] -- today's expense falls outside the range." },
  "ER-05": { purpose: "Omitting the range returns everything.", under: "GET /expenses with no from/to.", method: "2 expenses created via confirm. GET /expenses.", expected: "200, both present." },
  "ER-06": { purpose: "spec §5 -- expense list scoped to the logged-in user.", under: "GET /expenses scoping.", method: "User A creates 2, user B creates 1. GET /expenses as A.", expected: "Exactly 2 results, none matching B's expense id." },
  "ER-07": { purpose: "Summary is private.", under: "GET /expenses/summary with no Authorization.", method: "GET /expenses/summary, no header.", expected: "401." },
  "ER-08": { purpose: "spec §5 -- totals computed by the database must match what was actually saved.", under: "GET /expenses/summary totals (no date filter).", method: "2 expenses on 'Food' (10,15), 1 on 'Transport' (7), via confirm. GET /expenses/summary.", expected: "A row for Food totalling 25, a row for Transport totalling 7. FIXED (was 200 [] always -- expenseRepository.getExpenseTotalByCategory's raw aggregate $match never cast the string req.user.id to ObjectId; now passes)." },
  "ER-09": { purpose: "spec §5 -- summary scoped to the logged-in user.", under: "GET /expenses/summary scoping (no date filter).", method: "User A creates one expense (5), user B creates one (100). GET /expenses/summary as A.", expected: "Rows total 5, never 105. FIXED (was 0 for both users, same bug as ER-08; now passes)." },
  "ER-10": { purpose: "spec §5 -- a date range including today must include an expense recorded today.", under: "GET /expenses?from=&to= same-day boundary.", method: "Create one expense (server-stamped date = now). GET /expenses?from=<today>&to=<today>.", expected: "200, the expense is present.", divergence: "FIXED -- 200, the expense is present. queryExpenses now wraps from/to with startOfDay/endOfDay (UTC) helpers before comparing against the date field, instead of comparing raw strings. Was: Model.find() auto-cast the bare 'YYYY-MM-DD' string to midnight UTC that day, so $lte excluded anything recorded later the same day." },
  "ER-11": { purpose: "spec §5 -- a date range including today must include an expense recorded today, in the summary too.", under: "GET /expenses/summary?from=&to= same-day boundary.", method: "Create one expense (amount 10, date = now). GET /expenses/summary?from=<today>&to=<today>.", expected: "200, total 10.", divergence: "FIXED -- 200, total 10. getExpenseTotalByCategory's raw .aggregate() $match now wraps from/to with the same startOfDay/endOfDay helpers before comparing. Was: the raw aggregate compared the bare string to against a BSON Date and never matched." },

  // ---- FJ / DJ (e2e, real browser + real server) ----
  "FJ-01": { purpose: "Proves the actual user-facing loop end to end: type free text, see an AI-drafted expense, confirm, it is really saved. The one real (single) Gemini call in this whole QA pass.", under: "HomePage chat input/send -> POST /chat/messages (real AI) -> pending card -> Confirm -> POST /chat/confirm -> GET /expenses.", method: "Sign up through the UI; type 'spent 20 on coffee'; send; wait (generously, real network latency) for a Confirm button; click it; use the browser's own refreshToken cookie to mint a fresh access token and call GET /expenses directly.", expected: "A confirmable draft appears (intent was create-expense, not unknown). Both the typed text and the AI's reply appear in the thread. After confirming, the pending card clears and GET /expenses shows a real expense with amount===20." },
  "DJ-01": { purpose: "Proves that expenses recorded via chat show up on the dashboard -- the flagship 'chat writes, dashboard reads' contract from spec 00/02.", under: "POST /chat/confirm (seeding, no AI) -> UI login -> Dashboard nav link -> DashboardPage.", method: "Sign up via API; POST /chat/confirm create-expense twice (30 on Food, 20 on Transport, both dated today); log in through the UI; click the Dashboard nav link; read the rendered totals.", expected: "Per spec 02: Total spent reads ₪ 50.00; 'Food' and 'Transport' both appear in the breakdown.", divergence: "FIXED -- the dashboard now shows Total spent ₪ 50.00 with Food and Transport both in the breakdown, matching spec. Both the ObjectId bug (ER-08/09) and the date-boundary bug (ER-10/ER-11) it was masked-then-exposed-by are fixed. Note: this test's own locators were also rescoped -- the total figure and each category name legitimately appear twice on the page by design (summary card + total row; category breakdown + recent-expenses table), which is a Playwright strict-mode issue in the test, not a product bug." },

  // ---- CR (2026-09-16 pass: incomplete-input shapes behind bugs 1/9/10) ----
  "CR-01": { purpose: "Bug 1's exact root cause (undefined category crashes findByName), now guarded -- plus Finding A's messages-collection gap closed: full record checked, not just text.", under: "POST /chat/confirm create-expense with no category key.", method: "Sign up. POST /chat/confirm {intent:'create-expense', drafts:[{amount:10}]}.", expected: "400 {error:'Which category should this go under?'}. GET /expenses empty. GET /chat/messages has exactly one message, every field checked: role='assistant', author=userId, text exact." },
  "CR-02": { purpose: "An empty string must fail the same way an absent key does -- the falsy-check shape bug 1 lived in.", under: "POST /chat/confirm create-expense with category:''.", method: "POST /chat/confirm {intent:'create-expense', drafts:[{amount:10,category:''}]}.", expected: "Same 400 as CR-01. Nothing saved." },
  "CR-03": { purpose: "A category name that doesn't exist gets its own clear message, not a crash or a silent substitution.", under: "POST /chat/confirm create-expense with a nonexistent category name.", method: "POST /chat/confirm {intent:'create-expense', drafts:[{amount:10,category:'Spaceship Fuel'}]}.", expected: "400 {error:'No category named \"Spaceship Fuel\". Create it first?'}. Nothing saved. Exact message persisted to chat history, full record checked." },
  "CR-04": { purpose: "An ambiguous name (2+ matches) is rejected with candidates, never guessed.", under: "POST /chat/confirm create-expense where two categories share a name across different parents.", method: "Create 'Misc' as a main category and 'Misc' again under 'Transport'. POST /chat/confirm create-expense with category:'Misc'.", expected: "400, error starts with 'More than one category matches'. Nothing saved." },
  "CR-05": { purpose: "createManyExpenses' 'resolve every category before any write' contract, proven through the real HTTP path (not just the service unit test) -- this exact contract mismatch is bug 9's class.", under: "POST /chat/confirm create-expense, batch of 2, one bad category.", method: "POST /chat/confirm {intent:'create-expense', drafts:[{amount:5,category:'Food'},{amount:6,category:'Nonexistent'}]}.", expected: "400. GET /expenses still empty -- the valid first draft must not have been saved on its own (no partial save)." },
  "CR-06": { purpose: "Closes Finding A for the messages collection on the success path too -- the 'Done.' record's full shape, not just its text.", under: "POST /chat/confirm success -> the persisted 'Done.' message.", method: "POST /chat/confirm a valid create-expense draft. GET /chat/messages.", expected: "Exactly one new message: role='assistant', author=userId, text='Done.', createdAt within the last few seconds." },
  "CR-07": { purpose: "Bug 9 regression guard: create-category's parent name must resolve to a real id, never the string -- CC-08 only ever exercises a parent-less main category, so this shape was untested until now.", under: "POST /chat/confirm create-category with a name-based parent.", method: "Sign up (seeds 'Transport'). GET /categories for Transport's real _id. POST /chat/confirm {intent:'create-category', draft:{name:'Bike',parent:'Transport'}}.", expected: "200; changed.parent equals Transport's real _id string, never the literal 'Transport'." },
  "CR-08": { purpose: "A nonexistent parent name is rejected, not silently dropped or crashed on.", under: "POST /chat/confirm create-category with a nonexistent parent name.", method: "POST /chat/confirm {intent:'create-category', draft:{name:'Bike',parent:'Spaceships'}}.", expected: "400 {error:'No such category to add a subcategory to.'}. GET /categories does not include 'Bike'." },

  // ---- FS (2026-09-16 pass: multi-turn, corrected, abandoned, adversarial -- real browser + real AI) ----
  "FS-01": { purpose: "Conversation memory (bug 8 class): a missing amount answered in a bare follow-up turn must complete the same draft, and the stored expense must be field-correct (amount, real category, today's date -- bug 4 class).", under: "HomePage multi-turn chat -> POST /chat/messages (real AI) x2 -> Confirm -> POST /chat/confirm -> GET /expenses.", method: "Sign up via UI. Send 'spent money at the supermarket' (no amount) -- assert no Confirm button yet. Send '50'. Wait for Confirm, click it.", expected: "No Confirm after the first message. After '50', Confirm appears; confirming saves exactly one expense: amount===50, category is one of this user's real category ids, date is today." },
  "FS-02": { purpose: "The bug-fix file's 'ignored corrections' regression, end to end: a pushback before confirming must change the draft, not add a second one, and the reply must not repeat verbatim.", under: "HomePage chat -> correction mid-draft -> Confirm -> GET /expenses.", method: "Send 'spent 30 on transport'; wait for Confirm, record the assistant's reply text. Send 'actually make that 50' before confirming; wait for Confirm again, record the new reply. Click Confirm.", expected: "The second reply differs from the first. Exactly one expense is saved (not two), amount===50 (not 30)." },
  "FS-03": { purpose: "Several expenses in one message (spec §6) produce one confirmation covering all of them, each field-correct.", under: "HomePage chat, one message describing two expenses -> Confirm -> GET /expenses.", method: "Send 'I spent 20 on coffee and 15 on the bus'. Wait for the confirm card; assert at least 2 draft list items. Click Confirm.", expected: "Exactly 2 new expenses; amounts are {15,20}; both have a real category id and today's date." },
  "FS-04": { purpose: "Bug 10's first-half fix (prompt now forbids drafting against a nonexistent category) held under real AI, not just at the unit-prompt level.", under: "HomePage chat, a category ('Bike') that doesn't exist in the default set.", method: "Send 'spent 45 on a new bike'. Never click Confirm. Read the reply/draft (if any).", expected: "If a create-expense draft is shown, its category is one of this user's real categories, never the nonexistent 'Bike' verbatim. Either way, GET /expenses stays empty (nothing silently saved under a fake category)." },
  "FS-05": { purpose: "The pending draft is client-only memory; a reload must not resurrect or silently apply it, but persisted chat history (bug 5's fix) must survive.", under: "HomePage chat -> page.reload() before confirming.", method: "Send 'spent 20 on coffee'; wait for Confirm. Reload the page instead of confirming.", expected: "No Confirm button after reload. The user's message and the assistant's reply still appear (GET /chat/messages persisted them). GET /expenses is empty." },
  "FS-06": { purpose: "A real session boundary (logout + login) must preserve chat history and must not resurrect a pending draft, same guarantee as FS-05 via a different mechanism.", under: "HomePage chat -> Log out -> Log in -> /home.", method: "Send 'spent 15 on lunch'; wait for Confirm, do not click it. Log out via the navbar. Log back in with the same credentials.", expected: "The earlier exchange still appears in the thread. No Confirm button (fresh state). GET /expenses is empty." },
  "FS-07": { purpose: "Bug 6 is open by design (per this run's task instructions) -- this asserts the CURRENT behaviour so a regression in either direction is caught, not that the behaviour is correct.", under: "HomePage chat -- typing 'yes' instead of clicking Confirm.", method: "Send 'spent 20 on coffee'; wait for Confirm. Instead of clicking it, type 'yes' and send it as a new message.", expected: "No expense with amount===20 exists afterward -- typing 'yes' does not silently confirm the pending draft. If this starts failing because 'yes' now confirms, that is Adam having decided bug 6's fix direction, not a regression -- update this spec, don't just re-assert the old behaviour." },

  // ---- RG (2026-09-17 pass: regressions for the nine bug fixes) ----
  "RG-02": { purpose: "Bug 3's deterministic invariant: a negative amount can never reach the database, whatever the model does.", under: "backend/models/expenseModel.js amount min:0.01.", method: "POST /chat/confirm create-expense with amount -50, 0, -0.01 in turn, each on a fresh account.", expected: "400 in every case, error-contract shape, expense count unchanged before/after." },
  "RG-04": { purpose: "The other deterministic half of bug 3 -- an over-precise amount reaching the server directly.", under: "backend/models/expenseModel.js's validate() (Number(value.toFixed(2))===value).", method: "POST /chat/confirm create-expense with amount 12.345, 7.8912, 0.005 in turn.", expected: "400 in every case, nothing written. Regression spec recorded this as an open Adam blocker on 2026-09-17; re-verified against the current tree, where expenseModel.js now carries the precision validator." },
  "RG-05": { purpose: "Bug 1, the findings' number one: the app must never claim to have saved something it did not save.", under: "backend/ai/prompt.js + the reply the user reads.", method: "Fresh account, real UI: 'spent 22 at Aroma' -> wait for the draft -> reply 'yes' as a typed message (not the Confirm button). REDUCED to 2 repetitions this run (spec calls for 5) -- time budget; see final report.", expected: "The reply never asserts something was saved when nothing was; expense count always matches what actually happened, read from the database." },
  "RG-09": { purpose: "Bug 4: a reload must not log a just-logged-in user out (the refresh race).", under: "backend/bl/userService.js jti/replacedAt/RACE_GRACE_MS.", method: "Log in, reload 10 times in a row, asserting the session survives after each.", expected: "Logged in and chat rendered after all 10 reloads. (The original UJ-01 asserted this after only 1 reload and passed while reloading by hand logged the user out -- one repetition does not test a race.)" },
  "RG-10": { purpose: "The race mechanism directly: two concurrent refreshes with the same cookie must both succeed.", under: "userRepository.consumeRefreshToken's atomic findOneAndUpdate.", method: "Log in, fire two POST /users/refresh with the same cookie simultaneously (Promise.all).", expected: "Both 200. The racing pair creates at most one new RefreshToken row between them (not one each). The winning token still works afterward on a real protected route." },
  "RG-11": { purpose: "The property round 3 of bug 4's fix traded for the race tolerance: a rotated token must still die once the grace window closes.", under: "userService.js RACE_GRACE_MS, read from source via qa/harness/env.ts readRaceGraceMs() -- never hardcoded.", method: "Log in, refresh once, wait readRaceGraceMs()+1000ms, replay the OLD cookie.", expected: "401. (If RACE_GRACE_MS is ever widened, RG-09/RG-10 keep passing and this is the one that would catch it.)" },
  "RG-12": { purpose: "Bug 6: an empty chat message must be refused cleanly, never crash into a 500.", under: "backend/routes/chatRoute.js body('text').trim().notEmpty().", method: "POST /chat/messages with text as: '', '   ', '\\n\\t', omitted, null, 123, [], {}.", expected: "400 with {errors:[{field:'text',...}]} in every case, never 500, and error.log gains no line (a deliberate 4xx is not a server error)." },
  "RG-13": { purpose: "Bug 7's two slip-through variants: the ones most likely to pass a naive `!store && !description` check.", under: "backend/bl/expenseService.js requireLabel().", method: "POST /chat/confirm create-expense with (a) store/description both empty strings, (b) both whitespace-only, (c) both explicit null.", expected: "400 in all three, nothing written. (The neither-present variant was already covered by qa/tests/api/14-require-label-verify.test.ts check 1.)" },
  "RG-14": { purpose: "FR-39: the signup username-length message matches spec 03 exactly, including the en dash.", under: "backend/routes/userRoute.js signupValidators.", method: "POST /users/signup with a 2-character username; the expected message is read from userRoute.js's own source (not retyped) and cross-checked against docs/specs/03-api-contract.md.", expected: "400, {errors:[{field:'username', message:'Username must be 3–20 characters.'}]}." },
  "RG-15": { purpose: "EH-05 shape 1: a duplicate username names the username.", under: "backend/error_handling.js err.keyValue[0] fallback.", method: "Sign up, then sign up again with the same username and a different email.", expected: "Per the regression spec's design: 409 {error:'username already exists.'}. NOTE: qa/tests/api/01-signup.test.ts SU-12 already documents that a SEQUENTIAL duplicate normally hits the async signup VALIDATOR first (400, not 409) -- 409 needs a genuine race past that validator's own query. Both outcomes are asserted for; see the run report for which was observed." },
  "RG-16": { purpose: "EH-05 shape 1, the pair: a duplicate email names the email (proves the field is derived from err.keyValue, not hardcoded).", under: "Same as RG-15.", method: "Sign up, then sign up again with the same email and a different username.", expected: "409 {error:'email already exists.'} or the 400 validator path -- same caveat as RG-15." },
  "RG-17": { purpose: "The third EH-05 shape: a duplicate-key error with an unusual keyValue must not crash the error handler itself.", under: "error_handling.js's ?? {} and ?? \"That value\" fallbacks.", method: "Unit: call errorHandler directly with {code:11000} and no keyValue; {code:11000, keyValue:{}}; {code:11000, keyValue:{username:'a',email:'b'}}. (The first two shapes were already covered by the existing EH-05 block in unit/errorHandling.test.ts; this run adds the empty-object and two-key shapes.)", expected: "'That value already exists.' for the first two; 409 naming the FIRST key for the third, without throwing." },
  "RG-22": { purpose: "The fixes' blast radius: the ordinary create-expense flow must still work after nine bug fixes touched the shared prompt and services.", under: "The whole create-expense path, real UI.", method: "Fresh account: 'spent 50 at the supermarket and 32.5 on gas', Confirm.", expected: "Two expenses stored: amounts 50 and 32.5; labels matching what was said; category resolving to real, owned category ids; date = today; user = the signed-up user." },
  "RG-24": { purpose: "XC-13: helmet() must be strictly first, not merely present.", under: "backend/index.js middleware order.", method: "Static: parse index.js's app.use() calls, assert helmet is index 0. Runtime: helmet headers present on a 404, a 400, and a compressed 200.", expected: "Both hold. (The existing XC-13 test accepts EITHER order as a 'design note' -- this case is the strict pin the fix actually requires.)" },
  "RG-25": { purpose: "EH-05 changed a 409 body; the client only understands two error shapes -- a third would break field mapping.", under: "Every error path reachable from the API.", method: "Provoke 400 (validation), 400 (thrown), 401, 404 (unmatched route), 404 (cross-user ownership), 409/400 (duplicate) and validate each body against the shared zod schemas.", expected: "Every body is exactly {error} or {errors:[{field,message}]}." },
  "RG-26": { purpose: "error_handling.js was edited this round -- it decides what an error body contains. Extends existing secret-leak checks to the BUILT CLIENT BUNDLE, which nothing else scans.", under: "client/dist (if built).", method: "Walk client/dist's .js/.html/.css/.map files for a bcrypt hash prefix or a Google API key pattern.", expected: "None found. (Skipped, not failed, if client/dist does not exist.)" },
  "RG-29": { purpose: "Log separation must survive the error-handler change -- specifically the new 409 duplicate-key path must log at warn, not error.", under: "backend/.config/logger.js routing + error_handling.js.", method: "Provoke a deliberate 4xx and a duplicate-key 409/400; check backend/logs/app.log and error.log before/after each.", expected: "The deliberate 4xx and the duplicate path both leave error.log unchanged; app.log gains a line for the 4xx." },
  "RG-30": { purpose: "The findings: backend/ai/ had no output-length cap. Re-verifies whether one exists now.", under: "backend/ai/parseMessage.js MAX_RESPONSE_BYTES.", method: "Unit, fake client: a response body over 8192 UTF-8 bytes (padded via the 'reply' field, not 'store'/'description' -- those trip a separate 200-char suspicious-field guard first and would confound the byte-cap test specifically). Also a boundary check at exactly 8192 vs 8193 bytes.", expected: "Over the cap: refused (intent 'unknown', actionable reply), retried once first, never parsed as-is. Regression spec recorded this as 'expected to FAIL -- no such cap exists' on 2026-09-17; re-verified against the current tree, where parseMessage.js now defines MAX_RESPONSE_BYTES=8192." },
  "RG-31": { purpose: "The seam class at the layer the user sees: does the same expense read the same in the chat and on the dashboard?", under: "ChatScreen's money() vs the dashboard's money() -- both from client/src/lib/dashboardFormat.ts as of this pass.", method: "Fresh account, real UI: create an expense of 1234.5 through chat; read the amount printed on the confirm card; confirm; read the amount printed in the dashboard's table.", expected: "Identical strings. Regression spec recorded this as 'expected to FAIL -- ₪1234.50 on the card, ₪ 1,234.50 on the dashboard' on 2026-09-17; re-verified against the current tree, where ChatScreen.tsx now imports money from the same shared module." },

  // ---- RT (2026-09-17 exploratory: protected-route reload defect) ----
  "RT-01": { purpose: "The defect from the user's seat: reloading /dashboard must reload /dashboard, not silently land on /home via a login-then-bounce chain.", under: "authSlice initial status ('idle' not 'checking') + ProtectedRoute.", method: "Log in, navigate to /dashboard via the navbar, reload, wait for the page to settle.", expected: "Final URL /dashboard, dashboard rendered." },
  "RT-02": { purpose: "The mechanism, not just the end state -- a fix that flashes through /login before landing correctly would still pass an end-state check.", under: "Same as RT-01.", method: "Logged in, cold-load-via-reload /dashboard; sample location.pathname every 50ms for 4s.", expected: "Every sample is /dashboard; /login never appears in the sequence." },
  "RT-03": { purpose: "A bookmarked protected URL should open where it points, first navigation of a fresh context.", under: "ProtectedRoute with a pre-seeded valid session cookie.", method: "Copy a logged-in context's cookies into a brand-new browser context; navigate straight to /dashboard.", expected: "The dashboard renders, no redirect." },
  "RT-04": { purpose: "The neighbour check: /home's reload behaviour (which happened to already work, masking the bug) must not regress once /dashboard is fixed.", under: "Same as RT-01, different route.", method: "Log in, reload /home.", expected: "Final URL /home, chat rendered." },
  "RT-05": { purpose: "The opposite direction must not regress: a logged-out visitor on a protected URL must still reach /login.", under: "ProtectedRoute with no session.", method: "No session, open /dashboard then /home.", expected: "Both land on /login and stay there." },
  "RT-08": { purpose: "/ and an unknown path must behave sensibly in both auth states.", under: "App.tsx routing, PageNotFound.", method: "Load / logged out and logged in; load /no-such-page-xyz in both states.", expected: "/ -> /login logged out, /home logged in. /no-such-page-xyz -> the not-found page in both, never a redirect to /login when logged in." },

  // ---- SC (2026-09-17: shared component extraction, static checks) ----
  "SC-01": { purpose: "There are two money formatters and they disagree (₪1234.50 vs ₪ 1,234.50) -- catch any file that grows its own currency formatting instead of importing the shared one.", under: "Every module under client/src that renders a currency amount.", method: "Grep client/src for a currency-formatting expression (₪, .toFixed(2), toLocaleString) outside lib/dashboardFormat.ts; separately, every file containing a literal ₪ must import money from lib/dashboardFormat.", expected: "No offending files. STATUS: the first half now passes (ChatScreen.tsx was fixed to import the shared money() since the spec was written). The second half FAILS on client/src/components/LedgerSlip.tsx, which hardcodes a literal '₪ 82.00' instead of importing money() -- a real, minor finding (decorative login/signup illustration, not a live data path, but it will silently drift if the shared formatter's spelling ever changes)." },
  "SC-16": { purpose: "A regression in a shared component should be visible in both its callers at once -- guard the import graph so a new/removed caller is never silent.", under: "Every module importing ChatScreen / DashboardTotals / CategoryBreakdown.", method: "For each of the three components, list every non-test source file importing it.", expected: "ChatScreen -> HomePage, HowToUsePage. CategoryBreakdown -> DashboardPage, HowToUsePage. DashboardTotals -> DashboardPage only. NOTE: client-shared-components.md §5 originally listed HowToUsePage as a second DashboardTotals caller too; per the coordinator, lesson 4's animation was deliberately removed the same day and replaced with a link to the real dashboard, so that import is gone BY DESIGN -- not a defect, and this case is pinned to the current (single-caller) shape rather than the spec's now-stale claim." },
};
